# История переписки по данной ветке чата

Дата фиксации: 10.06.2026  
Проект: `/Users/ermolaevatatana/Documents/Дашборд`

> Примечание: это не дословная стенограмма каждого сообщения. Диалог был длинным, часть раннего контекста была сжата системой. Ниже записана максимально полная история по доступному контексту: цели, решения, изменения, вопросы, формулы и текущее состояние дашборда.

## 1. Стартовая задача

Пользователь попросила:

- проанализировать файлы в папке дашборда;
- использовать Context7 для анализа;
- подготовить версию для виртуального локального сервера, чтобы отслеживать изменения в браузере;
- работать с дашбордом на локальном сервере.

В проекте используются основные файлы:

- `index.php` — HTML-разметка дашборда;
- `style.css` — стили;
- `script.js` — логика чтения Excel, расчетов и отрисовки;
- `local-server.mjs` — локальный сервер;
- `Dashboard_report_Technical_unit.xlsm` — Excel-источник данных.

Был создан и использовался локальный сервер. Последние запуски выполнялись через:

```bash
node local-server.mjs
```

Дашборд открывался в Google Chrome. Пользователь отдельно попросила в дальнейшем всегда открывать дашборд именно в Chrome.

## 2. Изменения в карточках загрузки

В карточках с диаграммами были изменены блоки часов.

Изначально отображалось:

```text
1 912 / 7 900
факт от максимально возможных часов
```

Пользователь попросила заменить на структуру:

```text
1 912 | 7 900 | значения из N22/N27/N32/N37/N43
часы факт | плановые | простой
```

Позже были добавлены:

- отсутствия из `N23`, `N29`, `N35`, `N41`, `N48`;
- сверхурочные часы из `N24`, `N30`, `N36`, `N42`, `N49`;
- подзаголовок `Загрузка в часах`;
- бейдж сверхурочных с текстом вида `в т.ч. сверхурочно 5 ч.`;
- выравнивание бейджа по правому краю;
- закрепление заголовка `Загрузка в часах` по центру, чтобы он не сдвигался при появлении бейджа.

Были согласованы цвета:

- факт — зеленый;
- план — как был;
- простои — красный;
- отсутствия — светло-серый;
- подписи оставлены серыми;
- цвета часов сделаны ярче, но без окрашивания подписей.

## 3. Заголовки периодов и карточки

Для карточек с диаграммой названия стали браться из ячеек:

```text
A20, A25, A30, A35, A41
```

Чтобы не менять заголовки в коде.

Позже были уточнены подписи:

- для текущего дня: `фактические | плановые | простои | отсутствия`;
- для нарастающего итога: `фактические | плановые | простои | отсутствия`;
- для текущего месяца: `факт+прогноз | плановые | простои | отсутствия`;
- для прошлого месяца: `фактические | плановые | простои | отсутствия`;
- для следующего месяца: `прогноз | плановые | простои | отсутствия`.

Карточки `Текущий день` и `Нарастающий итог` временно менялись местами, затем были возвращены обратно.

## 4. Проценты и сравнение с прошлым периодом

Пользователь заметила, что проценты управлений за текущий день были равны `0`. Была найдена проблема с тем, что данные брались не из нужной строки. Уточнение: строка данных — `25`.

Текст вида:

```text
▼ 23.8% к прошлому месяцу
```

был перенесен внутрь диаграммы под общий процент и сокращен до:

```text
▼ 23.8%
```

Также обсуждалось, как считается этот процент.

Были эксперименты с расположением:

- `99%` и `▼ 23.8%` рядом;
- затем возврат к вертикальному расположению;
- увеличен отступ между строками.

## 5. Листы центров и строки простоев

На листе `ЦНГО` были добавлены строки с заголовком в столбце `B`:

```text
простои (часы)
```

Пользователь попросила добавить аналогичные строки на остальные листы для всех периодов, где были вставлены строки.

## 6. Блок проектов и таблица проектов

В блоке `Проекты` исправлялись:

- ширина блока, чтобы она была аналогична сумме блоков `Загрузка центра` и `Табель`;
- адаптация под разные экраны;
- бургер-меню для навигации, при этом дата и время должны оставаться справа;
- скрывшиеся статусы в таблице проектов;
- отображение дат: если `Дата завершения работ по договору` или `Прогнозируемая дата окончания` не указаны, выводить `-`, а не `30.12.1899`;
- индикатор по дате выводить только если обе даты известны;
- столбец индикатора по дате показывать только у проектов со статусом `в работе`.

На вкладке `ЛОИ` был исправлен отступ у названий управлений — выравнивание по левому краю без лишнего отступа.

Текст:

```text
сотрудников без административного персонала:
```

был сделан на 1 px больше и не жирным.

## 7. План-факт по проектам и визуализации

Пользователь попросила использовать таблицу на листе `Проекты` диапазона `A1:G`, чтобы построить воронку/этапы выполнения проекта по управлениям:

- `ЛОИ`;
- `УВП`;
- `КБ`;
- статусы `в работе` или `завершена`;
- дата завершения.

Перед размещением на сайте были сделаны картинки-макеты. Пользователь выбрала вариант 2 и попросила добавить пояснение, что синяя дата — дата окончания.

Затем обсуждалась будущая интеграция проектов из плана продаж для сверки план/факт. Был предложен вариант визуализации в стиле Ганта: плановые сроки и факт выполнения.

Пользователь согласовала макет, и он был добавлен на вкладку `Тех.блок` вместо блока `Проекты`, при этом на других вкладках блок `Проекты` остался неизменным.

Карточки вида:

```text
Всего проектов
2
на листе «Проекты»
```

были приведены к стилю согласованной картинки.

## 8. Расширение таблицы `Проекты`

Пользователь добавляла новые столбцы в Excel.

Сначала диапазон стал `A1:N`:

```text
ID
Проект
Центр
Лаборатория
Расчеты
РКД
Статус
Фактическая дата начала
Фактическая дата окончания
Фактические часы затрачены
План продаж
Плановая дата начала
Плановая дата окончания
Плановые часы
```

Затем были просьбы:

- оставить только 2026 год;
- вернуть 2025 и 2027, но показывать у них только январь и июль;
- в 2026 показывать январь, апрель, июль, октябрь;
- разделить статусы и метку `из плана`/`вне плана`;
- переносить длинное название проекта вместо троеточия;
- поправить подписи календаря;
- добавить серые пунктирные линии на границе годов 2025-2026 и 2026-2027;
- увеличить отступ между этапами;
- переименовать `Лаб.` в `ЛОИ`, `Расч.` в `УВП`;
- адаптировать блок план-факт под разные экраны;
- сделать блоки 2025 и 2027 меньше, по 1/4 ширины каждый, а 2026 шире;
- выровнять по левому краю статусы, план и отклонения.

Позже таблица расширилась до `A1:Q`: в столбцах 5-8 появились названия центров с их статусами. Нужно было вместо `РКД` подставлять названия центров из заголовков таблицы, но только тех, где есть значение. `-` считается пустым.

Пример цепочек этапов:

```text
ЛОИ - УВП - ЦНГО
ЛОИ - УВП - ЦНГО - WLGV
```

Для этапов были добавлены подсказки при наведении:

```text
ЦНГО — Центр нефтегазового оборудования
ЦДЦМ — Центр центробежных насосов, центробежных компрессоров и роторных машин
ЦДПМ — Центр деталей поршневых машин
ЦОТ — Центр осевых турбомашин
УВП — Управление виртуального полигона
ЛОИ — Управление лабораторными исследованиями
```

## 9. Общая загрузка центров по проектам

Пользователь попросила показать общую загрузку центров, зная:

- плановые сроки;
- плановые часы;
- фактические сроки;
- фактические часы.

Сначала была нарисована картинка для согласования. Пользователь попросила добавить на шкалу сроки, потом календарь периодов над шкалой, затем подписи периодов.

Позже изменения:

- общий блок со шкалой сделать меньше;
- оставшиеся блоки сделать шире;
- вместо сокращенных названий центров вывести полные названия;
- календарь в блоке `Сверка` сделать как в блоке `Общая загрузка`;
- сделать равномерную ширину блоков;
- сделать шире `Сроки: план / факт`, а `План`, `Факт` и другие метрики уже;
- если год вынесен наверх, месяц `январь` показывать без года;
- часы и подпись `ч` держать на одной строке;
- отклонения визуально подсветить стрелкой и цветом;
- добавить отступ между сроками и планом;
- затем увеличить отступ еще больше;
- заголовки и значения `План`, `Факт`, `Отклонение`, `Загрузка` выровнять по центру;
- аналогично выровнять заголовки и значения в блоке `Сверка`.

Также были установлены периоды:

- с июля 2025 по июль 2027;
- в блоках отображаются июль-январь 2025, 2026 как есть, январь-июль 2027;
- пунктирная линия должна начинаться ниже, чтобы не перекрывать слово `январь`;
- шкала должна отрезаться по границам периода.

## 10. Разделение проектов из плана и вне плана

Пользователь попросила:

- в блоке `Общая загрузка центров по проектам` сравнивать с планом только проекты, где стоит метка `из плана`;
- оставшиеся проекты вывести отдельно;
- проекты вне плана тоже объединить по центрам и вывести общие значения по срокам и часам;
- для внеплановых проектов сделать одну шкалу как факт;
- значения отклонений выводить со стрелкой, без знаков `+` и `-`;
- для проектов вне плана сделать градиентную шкалу: где больше всего проектов в месяц — краснее, где меньше — зеленее;
- вывести дату начала и завершения периода;
- добавить сумму количества проектов по статусам;
- количество по статусам нужно для каждого центра;
- на шкале отображать только проекты `в работе`, так как остальные статусы сейчас не дают загрузку на центры;
- подписать, что на шкале только проекты в работе.

Позже:

- вместо `в работе с: 25.11.2024` оставить только дату;
- аналогичную теплую шкалу добавить для блоков `из плана` и `сверка`, но там брать все статусы;
- в блоке `Общая загрузка центров по проектам` отобразить шкалу `проекты` вместо шкалы `факт`, но оставить название `факт`;
- в блоке `Сверка плана` проекты по месяцам оставить только `в работе`, добавить подписи месяцам и год ниже;
- сдвинуть названия месяцев вправо;
- вместо `Без указанного центра` выводить информацию по `ЛОИ` и `УВП` отдельно;
- легенду, которая больше не соответствовала теплой шкале, убрать или переделать.

## 11. Блок `Внеплановая нагрузка`

Обсуждались возможные дополнительные аналитические блоки. Был предложен и нарисован блок `Внеплановая нагрузка`.

Пользователь попросила добавить только этот блок. Затем уточнила:

- сделать как на картинке;
- две шкалы должны быть на одной строке;
- слева сумма часов плана;
- справа сумма часов вне плана.

Позже пользователь попросила полностью убрать блок `Внеплановая нагрузка`.

Блок был удален из отображения.

## 12. Проектная структура загрузки по центрам

Был добавлен блок:

```text
Проектная структура загрузки по центрам
```

Он показывает разделение фактических часов центра:

- из плана продаж;
- вне плана;
- всего.

Пользователь попросила:

- добавить последний столбец с процентным соотношением между проектами из плана и вне плана;
- заменить желтый цвет на оранжевый;
- потом сделать оранжевый чуть тусклее;
- в этом блоке часы `из плана` сделать синими;
- часы `вне плана` сделать оранжевыми, чтобы совпадали со шкалой.

Было сделано:

- добавлен столбец `Соотношение`;
- вывод вида `72% / 28%`;
- оранжевый акцент приглушен;
- значения часов окрашены в соответствующие цвета.

## 13. Переход к плоским таблицам техблока

Пользователь попросила использовать таблицы:

- `Загрузка_ТехБлок`;
- `Проекты_ТехБлок`;
- `Сотрудники_ТехБлок`;
- `Табель`;
- позже `План_продаж`;
- `Справочник` для правил статусов `T2:V14`.

Задача: связать данные по `ID`, `ФИО`, центрам и управлениям, чтобы блок `План-факт по проектам` собирался автоматически.

Пользователь уточнила смысл столбцов.

### `Проекты_ТехБлок`

- `Центр` — только центр: `ЛОИ`, `УВП`, `ЦНГО`, `ЦДЦМ`, `ЦДПМ`, `ЦОТ`;
- `Управление` — управления, входящие в центры;
- `Дата завершения работ по договору` — дата, когда должны быть выполнены работы по проекту;
- `ПРОГНОЗ окончания` — когда по плану закончится проект;
- `Количество ПЛАНовых часов` — задает руководитель проектов; часы должны быть потрачены на проект в определенном центре и управлении;
- `Количество ФАКТических часов` — сумма часов, должна сходиться с `Загрузка_ТехБлок`;
- `ФАКТ начало` — когда начались работы по проекту; если пусто, берется первая дата из `Загрузка_ТехБлок` по тому же центру и управлению;
- `ФАКТ окончание` — аналогично факту начала;
- `Количество ПРОГНОЗируемых часов` — будущие часы, сколько планируем еще потратить, начиная с сегодняшнего дня;
- `Задействовано сотрудников` — актуальное количество задействованных сотрудников.

### `Загрузка_ТехБлок`

- `Управление` — управления, входящие в центры;
- `Табель` — часы в день по календарю;
- `Отсутствия` — сотрудник отсутствовал; буква `к` не считается;
- `Прогноз` — прогнозные будущие часы, если `0` — факт;
- `Административный персонал` — если `1`, не использовать для расчетов;
- `Дата увольнения` — отслеживать, когда человек работал и когда перестал.

### `Сотрудники_ТехБлок`

- `Центр` — только центр;
- `Управление` — управления;
- `Отдел` — входит в управление;
- `Административный персонал` — если `1`, не использовать;
- `Дата увольнения` — учитывать период работы;
- `Ставка` — нужна для расчета затраченного времени.

Были скорректированы расчеты:

- завершенные и отмененные проекты исключаются;
- плановые часы из `Проекты_ТехБлок` используются только для проектов вне плана продаж;
- для проектов из плана плановые часы берутся из `План_продаж`;
- факт для проектов из плана должен считаться из `Загрузка_ТехБлок`;
- административный персонал `=1` исключается.

Обсуждались и исправлялись вопросы:

- откуда берутся плановые часы `816 784`;
- почему сумма плановых часов стала `209 639`, а не `165 349`;
- откуда берутся фактические часы `54 548 ч`;
- откуда число `60` — количество планов;
- как считается сумма `7 448 ч`;
- как считается план `7 448 ч` для `ЦНГО`;
- что делать, пока в плане продаж нет разделения часов по центрам.

В итоге было уточнено:

- план — из листа `План_продаж`;
- факт — сумма фактических часов из `Загрузка_ТехБлок` по проектам, которые есть в плане продаж;
- вне плана — плановые часы из `Проекты_ТехБлок`.

## 14. Тепловая шкала по часам

Пользователь спросила, как правильно брать часы для тепловой шкалы, если известны:

- затраченный факт;
- плановые часы;
- количество людей в период;
- отсутствия;
- сколько часов в месяц можно было потратить.

Была объяснена логика:

```text
загрузка месяца = часы нагрузки / доступные часы
```

Где доступные часы:

```text
Табель × ставка сотрудников − отсутствия
```

Из расчетов исключается административный персонал `=1`.

Для проектов вне плана плановые часы берутся из `Проекты_ТехБлок`.

Были разобраны вопросы:

- почему для внеплановых проектов `ЦНГО` в декабре 2025 доступно `439 ч`;
- как разложить сумму отсутствий `10 073,5 ч`;
- как разложить сумму `10 512 ч`;
- как считается план для внеплановых проектов, например `ЦНГО` план `10 741`.

## 15. Фактическая загрузка по месяцам

Пользователь просила:

- вынести `Фактическая загрузка по месяцам` наверх;
- затем проекты из плана;
- затем вне плана;
- затем каждый проект, но последний блок сделать свернутым.

Позже было замечено, что в мае в `Фактической загрузке по месяцам` загрузка `46%`, а в карточке текущего месяца `59%`. Пользователь уточнила, что карточка считает:

```text
сумма факт часов / сумма макс загрузки с учетом отсутствий и переработки
```

Аналогично должны считаться месяцы.

Потом пользователь попросила вернуть как было и отдельно спросила, как считаются:

- план;
- факт;
- доступно;
- загрузка.

Были предложены улучшения анализа, сделана картинка, затем пользователь попросила добавить:

- разбивку проектной структуры загрузки по центрам;
- сравнение проектов из плана по срокам и часам с фактом выполнения.

Блок `Фактическая загрузка по месяцам` был добавлен на вкладки центров перед блоком `Динамика загрузки по кварталам`.

Позже тот же стиль был перенесен на вкладку `Тех.блок`.

Пользователь уточнила, что период должен быть не 3 месяца, а тот же, что был раньше. Было исправлено: на `ТехБлок` период отображается шире, по диапазону анализа.

## 16. Общая загрузка по месяцам: текущая загрузка и прогноз

Пользователь предложила теоретически обсудить, какие статусы брать в `Общую загрузку`.

Была дана рекомендация:

- основной показатель — только проекты `в работе`;
- прогноз/очередь — отдельным слоем, начиная со следующего месяца;
- завершенные и отмененные не учитывать;
- завершенные можно использовать только для истории, но не для текущей нагрузки.

Был нарисован макет:

- для вкладки центра — один центр;
- для `ТехБлок` — общая агрегированная загрузка без разбивки по центрам;
- шкала разделена по месяцам;
- строка `в работе`;
- строка `прогноз / очередь`;
- затем завершенные/история были убраны.

Пользователь попросила:

- проекты `в работе` сделать шкалой повыше;
- проценты писать внутри кирпичиков;
- шкалу прогноза оставить ниже;
- потом добавить проценты и в прогнозную шкалу, но по высоте она должна быть меньше, чем `в работе`.

Было внедрено:

- `Общая загрузка по месяцам`;
- строка `в работе` высотой больше;
- строка `прогноз / очередь` ниже и тоньше;
- проценты внутри обоих типов месячных блоков;
- прогноз/очередь начинается со следующего месяца;
- завершенные и отмененные проекты исключаются.

## 17. Перестановка блоков

Пользователь попросила поменять порядок:

1. `Общая загрузка по месяцам`;
2. `Динамика загрузки по кварталам`;
3. `Проектная структура загрузки по центрам`;
4. остальное оставить как было.

На вкладке `ТехБлок` квартальный график был перенесен внутрь потока блока `План-факт по проектам`, чтобы порядок стал правильным. Старый дубликат квартального графика на `ТехБлок` был скрыт.

На вкладках центров порядок уже был корректным:

```text
Общая загрузка по месяцам
Динамика загрузки по кварталам
```

## 18. Защита дашборда

Пользователь спросила, как защитить дашборд от лишних глаз.

Обсуждались варианты:

- держать только на `127.0.0.1`;
- использовать VPN;
- Basic Auth;
- корпоративный SSO;
- HTTPS;
- не отдавать Excel-файл напрямую без авторизации.

Была внедрена опциональная защита в `local-server.mjs` через Basic Auth.

Переменные окружения:

```bash
DASHBOARD_AUTH=1
DASHBOARD_USER="dashboard"
DASHBOARD_PASSWORD="ваш_пароль"
```

Запуск с паролем:

```bash
DASHBOARD_AUTH=1 DASHBOARD_USER="dashboard" DASHBOARD_PASSWORD="ваш_пароль" node local-server.mjs
```

Затем пользователь попросила пока оставить дашборд без пароля.

В результате:

- механизм защиты остался в коде;
- по умолчанию авторизация отключена;
- чтобы включить, нужно явно указать `DASHBOARD_AUTH=1`.

## 19. Локальный сервер

Дашборд запускался на локальном сервере. Последний зафиксированный запуск:

```text
http://127.0.0.1:8080/
```

Авторизация отключена.

Пользователь просила открывать дашборд всегда в Google Chrome.

## 20. Сверхурочные часы

Пользователь спросила, как считать часы сверхурочной работы формулой в Excel.

Была предложена логика:

```text
сверхурочные = факт часы сотрудника за день - норма часов сотрудника за день
```

Если значение меньше нуля, брать `0`.

Норма:

```text
Табель × Ставка
```

Правильный расчет:

```text
Сверхурочно за период =
сумма по каждому сотруднику и дате:
МАКС(0; факт часы за день - табель × ставка)
```

Исключать:

- административный персонал;
- прогнозные строки;
- отсутствия, кроме `к`;
- строки после даты увольнения.

## 21. Архитектура данных как база данных

Пользователь попросила объяснить, как переделать Excel-таблицы, чтобы дашборд мог пользоваться ими как базой данных.

Была предложена архитектура:

### Справочники

- `dict_centers`;
- `dict_managements`;
- `dict_departments`;
- `dict_project_statuses`;
- `dict_project_types`;
- `dict_absence_types`.

### Основные сущности

- `dim_employees`;
- `dim_projects`;
- `project_work_scope`.

### Факты

- `fact_work_hours`;
- `fact_absences`;
- `fact_sales_plan`;
- `fact_sales_plan_by_center`;
- `dim_calendar`.

Основные ключи:

```text
project_id
employee_id
center_code
management_code
calendar_date
```

Было рекомендовано:

- не использовать ФИО как главный ключ;
- использовать `employee_id`;
- использовать `project_id`;
- все таблицы делать плоскими;
- один заголовок в первой строке;
- не использовать объединенные ячейки;
- не хранить сводки внутри сырьевых таблиц.

Примеры переименования столбцов:

```text
Часы → hours
Дата → work_date
Табель → timesheet_hours
ФИО → employee_name
Табельный номер → employee_id
ID → project_id
Центр → center_code
Управление → management_code
Отдел → department_name
Вид проекта → project_type_code
Вид работ → work_type_name
Прогноз → is_forecast
Отсутствия → absence_code
Административный персонал → is_admin
Дата увольнения → termination_date
Ставка → rate
Количество ПЛАНовых часов → planned_hours
Количество ФАКТических часов → actual_hours_manual
Количество ПРОГНОЗируемых часов → forecast_hours_manual
Дата завершения работ по договору → contract_end_date
ПРОГНОЗ окончания → forecast_end_date
ФАКТ начало → fact_start_date
ФАКТ окончание → fact_end_date
```

## 22. Вкладка `Расчеты`

Пользователь попросила научиться считывать данные с плоской таблицы и добавить вкладку, где будут расписаны расчеты, чтобы понимать, откуда какие данные взялись.

Была добавлена вкладка:

```text
Расчеты
```

Она не требует отдельного листа Excel. Это отдельный экран дашборда, который собирает данные из существующих плоских таблиц:

- `Проекты_ТехБлок`;
- `Загрузка_ТехБлок`;
- `Сотрудники_ТехБлок`;
- `Табель`;
- `План_продаж`;
- `Справочник`.

На вкладке отображается:

- список плоских таблиц и их заголовков;
- количество строк и столбцов;
- краткие формулы расчета;
- сверка по центрам;
- факт, фонд, загрузка, прогноз;
- проекты из плана и вне плана;
- раскрываемая расшифровка по проектам;
- источники плана и факта.

Вкладка добавлена в меню рядом с остальными разделами.

## 23. Текущие важные файлы и изменения

Основные измененные файлы:

- `index.php`;
- `style.css`;
- `script.js`;
- `local-server.mjs`;
- `mockups/general-load-status-model.html`;
- `mockups/general-load-status-model.png`;
- `mockups/render_general_load_status_model.py`;
- `История_переписки.md`.

## 24. Текущее состояние

На текущий момент:

- дашборд работает на локальном сервере;
- авторизация выключена по умолчанию;
- авторизацию можно включить через `DASHBOARD_AUTH=1`;
- дашборд открывается в Google Chrome;
- добавлена вкладка `Расчеты`;
- блок `Общая загрузка по месяцам` показывает `в работе` и `прогноз / очередь`;
- блок `Проектная структура загрузки по центрам` содержит соотношение из плана / вне плана;
- блок `Внеплановая нагрузка` удален;
- план-факт по проектам собирается из плоских таблиц техблока;
- `План_продаж` используется как источник плановых часов для проектов из плана;
- факт берется из `Загрузка_ТехБлок`;
- административный персонал исключается из расчетов;
- завершенные и отмененные проекты исключаются из текущей загрузки.

## 25. Продолжение работы в папке `Дашборд - отчет по Excel`

Дата фиксации: 11.06.2026 08:58 MSK

Пользователь попросила продолжить работу в новой папке:

```text
/Users/ermolaevatatana/Documents/Дашборд - отчет по Excel
```

В качестве истории решений использован файл `История_переписки.md`.

Что было сделано:

- прочитан текущий контекст проекта и структура новой папки;
- проверены основные файлы `index.php`, `script.js`, `style.css`, `local-server.mjs`;
- локальный сервер был поднят через `node local-server.mjs`;
- дашборд проверялся по адресу `http://127.0.0.1:8080/`;
- подтверждено, что авторизация по умолчанию выключена;
- дашборд открыт в Google Chrome.

## 26. Удаление блока `Проектная структура загрузки по центрам`

Пользователь попросила полностью убрать блок:

```text
Проектная структура загрузки по центрам
```

Было сделано:

- из `script.js` удален вызов `renderProjectLoadStructureByCenterBlock(centerLoad)`;
- удалены функции рендера этого блока:
  - `renderProjectLoadStructureByCenterBlock`;
  - `getProjectLoadStructureByCenterRows`;
  - `renderProjectLoadStructureByCenterRow`;
- из `style.css` удалены стили и адаптивные правила `project-structure-*`;
- проверено, что `script.js` проходит `node --check`;
- проверено, что серверные `script.js` и `style.css` больше не содержат `project-structure` и текст заголовка блока.

## 27. Правило использования Context7

Пользователь попросила:

```text
в своей работе всегда используй context7
```

Принято правило:

- в дальнейшей работе по проекту использовать Context7 перед изменениями, особенно при работе с JavaScript, DOM, Chart.js, SheetJS, PHP/JS-паттернами, Markdown и другой документацией;
- после Context7 продолжать читать локальный код и вносить изменения по фактической структуре проекта.

## 28. Удаление блока `Проекты вне плана`

Пользователь попросила полностью удалить блок:

```text
Проекты вне плана
```

Было сделано:

- через Context7 проверен общий подход к работе с JavaScript/DOM и шаблонными строками;
- из `script.js` удален вызов `renderOutsidePlanProjectLoadBlock(centerLoad.outsidePlan, startDate, endDate)`;
- удалены функции рендера блока `Проекты вне плана`:
  - `renderOutsidePlanProjectLoadBlock`;
  - `renderOutsidePlanStatusSummary`;
  - `renderOutsidePlanCenterRow`;
  - `renderOutsidePlanCenterStatusSummary`;
- из `style.css` удалены стили таблицы, строк, календаря, статусов и метрик блока `outside-plan-*`;
- оставлены только `outside-plan-heatmap-*`, потому что эта тепловая шкала переиспользуется в соседнем блоке `Проекты из плана: сроки и часы по центрам`;
- проверено, что `script.js` проходит `node --check`;
- локальный сервер был запущен, дашборд открыт в Chrome.

## 29. Вкладка `ПП` / `Производственная программа`

Пользователь попросила:

```text
добавь вкладку "ПП" - с расшифровкой "Производственная программа".
перенеси туда полностью блок "План-факт по проектам" и все его содержимое
```

Было сделано:

- через Context7 проверен подход к навигации на vanilla JavaScript через `data-*`, `querySelectorAll`, `classList` и обработчики кликов;
- в `index.php` добавлена кнопка навигации:

```html
<button class="nav-btn" data-sheet="ПП" title="Производственная программа">ПП</button>
```

- в `index.php` добавлен отдельный контейнер страницы:
  - `production-program-content`;
  - заголовок `Производственная программа`;
  - блок `production-program-projects-block`;
  - контейнер `production-program-projects-container`;
- в `script.js` добавлена обработка вкладки `ПП` в `loadSheetData`;
- вкладка `ПП` сделана служебной страницей без отдельного листа Excel;
- для расчетов вкладки `ПП` используется лист `ТехБлок` как источник `worksheetCache`, а план-факт собирается из плоских таблиц:
  - `Проекты_ТехБлок`;
  - `Загрузка_ТехБлок`;
  - `Сотрудники_ТехБлок`;
  - `Табель`;
  - `План_продаж`;
  - `Справочник`;
- добавлена функция `switchToProductionProgramMode`;
- добавлена функция `renderProductionProgramPlanFact`;
- блок `План-факт по проектам` перенесен из вкладки `ТехБлок` во вкладку `ПП`;
- на вкладке `ТехБлок` снова отображается обычный блок `Проекты`;
- квартальный график внутри вкладки `ПП` получил отдельный id:

```text
production-program-quarterly-trend-chart
```

Чтобы не конфликтовать с графиком `quarterly-trend-chart` на других вкладках.

Проверка:

- `script.js` проходит `node --check`;
- во встроенном браузере проверено:
  - вкладка `ПП` активируется;
  - в меню отображается `ПП`;
  - заголовок страницы: `Производственная программа`;
  - внутри есть `План-факт по проектам`;
  - внутри есть `Общая загрузка по месяцам`;
  - внутри есть `Проекты из плана: сроки и часы по центрам`;
  - внутри есть `Сверка по каждому проекту`;
  - график имеет id `production-program-quarterly-trend-chart`;
  - на вкладке `ТехБлок` текста `План-факт по проектам` больше нет;
  - на вкладке `ТехБлок` заголовок блока снова `Проекты`;
  - ошибок консоли не было.

## 30. Новое правило ведения истории

Пользователь попросила:

```text
дописывай каждый раз свои действия в файл История_переписки.md
```

Принято правило:

- после каждого выполненного изменения или значимого действия по проекту дописывать краткую фиксацию в `История_переписки.md`;
- фиксировать не только результат, но и ключевые технические решения, измененные файлы и проверки;
- продолжать использовать Context7 перед работой, как было отдельно согласовано.

## 31. Перемещение вкладки `ПП` в конец меню

Дата фиксации: 11.06.2026 08:59 MSK

Пользователь попросила:

```text
перемести вкладку ПП в самый конец
```

Было сделано:

- через Context7 проверен общий принцип: порядок кнопок в HTML задает визуальный порядок навигации и порядок обхода DOM;
- в `index.php` кнопка вкладки `ПП` перенесена из позиции сразу после `ТехБлок` в конец списка навигации;
- новый порядок меню:

```text
ТехБлок → ЦНГО → ЦДЦМ → ЦДПМ → ЦОТ → УВП → ЛОИ → НК/ТК → Расчеты → ПП
```

- так как переключение стрелками использует порядок `.nav-btn` в DOM, вкладка `ПП` теперь также последняя в клавиатурной навигации.

## 32. Удаление блока `Проекты` со вкладки `ТехБлок`

Дата фиксации: 11.06.2026 09:07 MSK

Пользователь попросила:

```text
убери блок "Проекты" со вкладки ТехБлок
```

Было сделано:

- через Context7 проверен общий подход к условному скрытию DOM-секции через JavaScript;
- в `script.js` изменена функция `updateProjects`;
- если активная вкладка `currentSheet === 'ТехБлок'`, блок `.block-projects` скрывается через `display: none`;
- при скрытии также очищается `#projects-container` и прячется `#indicators-legend`;
- для остальных вкладок `.block-projects` снова показывается и обычная логика блока `Проекты` сохраняется;
- `script.js` проверен через `node --check`;
- во встроенном браузере проверено:
  - на вкладке `ТехБлок` `.block-projects` имеет `display: none` и высоту `0`;
  - на вкладке `ЦНГО` блок `Проекты` снова отображается;
  - ошибок консоли не было.

## 33. Источники данных блока `Общая загрузка по месяцам`

Дата фиксации: 11.06.2026 09:13 MSK

Пользователь спросила:

```text
откуда берутся данные для блока "Общая загрузка по месяцам"?
```

Разобрана цепочка в `script.js`.

Вывод:

- на вкладке `ПП` блок строится функцией `renderPlanFactHeatmapSummary`;
- при входе на вкладку `ПП` в качестве `worksheetCache` используется лист Excel `ТехБлок`, поэтому подписи и фонд часов берутся из месячных строк этого листа;
- текущий месяц:
  - подпись месяца — `A33`;
  - факт/загрузка по листу — `N32`;
  - фонд/всего часов — `N33`;
  - процент — `N37`;
- прошлый месяц:
  - подпись — `A39`;
  - факт/загрузка — `N38`;
  - фонд/всего — `N39`;
  - процент — `N44`;
- следующий месяц:
  - подпись — `A46`;
  - факт/прогноз — `N45`;
  - фонд/всего — `N46`;
  - процент — `N50`;
- исторические месяцы берутся блоками по 5 строк, начиная с `A55`/`N54:N58`, далее шаг `5` строк, максимум `24` блока;
- `Факт часов` в карточке блока не берется напрямую из `N32`, а пересчитывается из плоской таблицы `Загрузка_ТехБлок` по проектам со статусом `В работе`;
- факт собирается по `ID`, `Дата`, `Часы`, `Центр`, `Управление`, `ФИО`, `Прогноз`, `Отсутствия`, `Административный персонал`, `Дата увольнения`;
- административный персонал исключается;
- отсутствия, кроме `к`, исключаются из проектных часов;
- строки после даты увольнения исключаются;
- если `Прогноз` заполнен/не равен `0`, строка идет в прогноз, а не в факт;
- `В работе` считается как:

```text
факт часов за месяц / фонд часов за месяц
```

- `Прогноз / очередь` считается с месяца, следующего за текущим, как:

```text
прогнозные часы за месяц / фонд часов за месяц
```

- завершенные и отмененные проекты не участвуют в текущей загрузке;
- для проектов не в работе, если нет прогнозных часов, может использоваться fallback из плановых месячных часов проекта.

## 34. Расчет фонда часов `Общая загрузка по месяцам` из плоских таблиц

Дата фиксации: 11.06.2026 09:43 MSK

Пользователь попросила:

```text
давай рассчитывать фонд часов из таблиц:
Сотрудники_ТехБлок, где указаны все сотрудники, управления, центр, админ персонал, дата увольнения, по которым можно исключить лишних сотрудников
лист Табель где можно посчитать кол-во рабочих дней и часов за разные периоды
лист Загрузка_ТехБлок можно узнать по каким проектам в какой день сколько часов потратил каждый сотрудник (правила с прогнозом и командировками и подобными сохраняются)
лист Проекты_ТехБлок где будет вся информация о проекте
```

Было сделано:

- через Context7 проверен подход к агрегации данных через `Map` и суммирование по ключам месяцев;
- в `script.js` изменена функция `getCenterMonthlyLoadMonthsForRange`;
- раньше фонд часов для верхнего блока `Общая загрузка по месяцам` брался из месячных строк листа `ТехБлок` (`N33`, `N39`, `N46` и исторические блоки);
- теперь фонд часов считается через существующую табличную функцию `getTechBlockCenterCapacityByMonth`;
- расчет фонда по месяцам:
  - берет календарные рабочие часы из листа `Табель`;
  - берет сотрудников из `Сотрудники_ТехБлок`;
  - исключает административный персонал;
  - учитывает `Дата выхода`, если она есть;
  - учитывает `Дата увольнения`;
  - умножает часы табеля на `Ставка`;
  - вычитает отсутствия из `Загрузка_ТехБлок`;
  - командировка `к` не считается отсутствием и не вычитает фонд;
  - суммирует доступные часы по всем центрам в общий фонд месяца;
- правила факта и прогноза из `Загрузка_ТехБлок` сохранены:
  - административный персонал исключается;
  - отсутствия, кроме `к`, исключаются из проектных часов;
  - строки после даты увольнения исключаются;
  - строки с `Прогноз` не равным `0` идут в прогноз, а не в факт;
- `Проекты_ТехБлок` продолжает быть источником структуры проекта, статусов, центров, управлений и связи с `ID`;
- `script.js` проверен через `node --check`;
- во встроенном браузере проверена вкладка `ПП`:
  - блок `Общая загрузка по месяцам` отображается;
  - факт за май остался `14 671 ч`;
  - фонд за май стал `32 006 ч`;
  - загрузка стала `46%`;
  - прогноз/очередь за июнь стал `21%`;
  - ошибок консоли не было.

## 35. Компоновка шкалы `Общая загрузка по месяцам`

Дата фиксации: 11.06.2026 09:49 MSK

Пользователь попросила:

```text
заголов "Общая загрузка
в работе и будущая потребность" вынеси наверх, над шкалой, а сама шкала пусть подвинется в самое лево, чтобы весь диапазон месяцев поместился на одну строку
```

Было сделано:

- через Context7 проверен подход к CSS Grid/Flex layout, где элемент может занимать всю ширину контейнера;
- в `style.css` изменена компоновка `.center-monthly-load-heatwrap`:
  - вместо двухколоночной сетки используется вертикальный flex;
  - заголовок `Общая загрузка / в работе и будущая потребность` расположен над шкалой;
  - шкала получила всю доступную ширину контейнера;
- в `script.js` функция `renderMonthlyLoadScale` теперь задает CSS-переменную `--monthly-load-count` по количеству месяцев;
- в `style.css` месячные ячейки строятся через:

```css
grid-template-columns: repeat(var(--monthly-load-count, 1), minmax(34px, 1fr));
```

- это убирает перенос месяцев на вторую строку и держит весь диапазон в одном ряду;
- во встроенном браузере проверено:
  - заголовок находится выше шкалы;
  - ряд `в работе` содержит 24 месяца и не переносится;
  - ряд `прогноз / очередь` содержит 24 месяца и не переносится;
  - ошибок консоли не было.

## 36. Уточнение по проектам в расчете `Общая загрузка по месяцам`

Дата фиксации: 11.06.2026 10:14 MSK

Пользователь спросила:

```text
для расчетов ты взял только те проекты, которые есть на листе План_продаж?
```

Проверена цепочка расчета в `script.js`.

Ответ:

- нет, для верхнего блока `Общая загрузка по месяцам` берутся не только проекты из `План_продаж`;
- базовая выборка проектов начинается с листа `Проекты_ТехБлок`;
- проекты группируются по `ID`;
- `План_продаж` используется только как дополнительный источник для проектов, чей `ID` найден в плане продаж:
  - такие проекты получают `isPlanned = true`;
  - плановые даты и плановые часы берутся из `План_продаж`;
- проекты, которых нет в `План_продаж`, но которые есть в `Проекты_ТехБлок`, тоже попадают в расчет, если проходят фильтры;
- для `Факт часов` / строки `в работе` берутся проекты со статусом `active` / `В работе`;
- факт по ним берется из `Загрузка_ТехБлок` по месячным часам;
- для `Прогноз / очередь` берутся проекты не `Завершена` и не `Отменена`;
- для проектов без прогнозных часов может использоваться fallback из плановых месячных часов проекта;
- завершенные и отмененные проекты исключаются из текущей загрузки.

Важное различие:

- блок `Проекты из плана: сроки и часы по центрам` ниже действительно строится только по проектам `isPlanned`;
- верхний блок `Общая загрузка по месяцам` показывает общую нагрузку по всем релевантным проектам из `Проекты_ТехБлок`, а не только по `План_продаж`.

## 37. Верхний блок `Общая загрузка по месяцам` только по плану продаж

Дата фиксации: 11.06.2026 13:04 MSK

Пользователь попросила:

```text
давай для расчета верхнего блока брать только задачи, которые совпадают с планом продаж, чтобы не смешивать все в одну кучу
```

Было сделано:

- через Context7 проверен подход с `Array.prototype.filter`: фильтр создает отдельную выборку и не мутирует исходный массив проектов;
- в `script.js` изменена функция `renderPlanFactHeatmapSummary`;
- теперь верхний блок `Общая загрузка по месяцам` сначала формирует `plannedProjects`:

```js
const plannedProjects = projects.filter(project => project.isPlanned);
```

- строка `в работе` считает факт только по активным проектам из `plannedProjects`;
- строка `прогноз / очередь` считает будущую потребность только по `plannedProjects`;
- фонд часов остается расчетным по сотрудникам/табелю, как было сделано ранее;
- подзаголовок блока изменен на:

```text
ТехБлок: проекты из плана продаж в работе / фонд часов. Прогноз и очередь показываются со следующего месяца.
```

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер `http://127.0.0.1:8080/` отвечает `200 OK`;
- во встроенном браузере открыта вкладка `ПП`;
- блок `Общая загрузка по месяцам` отображается на вкладке `ПП`;
- после фильтрации только по `План_продаж` KPI стали:
  - `Факт часов`: `6 066 ч`;
  - `Фонд часов`: `32 006 ч`;
  - `В работе`: `19%`;
  - `Прогноз / очередь`: `13% июнь`;
- шкала содержит 24 месяца в строках `в работе` и `прогноз / очередь`;
- ошибок консоли не было.

## 38. Перенос `Динамика загрузки по кварталам` на вкладку `ТехБлок`

Дата фиксации: 11.06.2026 14:04 MSK

Пользователь попросила:

```text
Динамика загрузки перенеси его на вкладку ТехБлок под блоками, что там есть
```

Было сделано:

- через Context7 проверен подход с рендером секции в существующий DOM-контейнер;
- в `script.js` изменена функция `renderQuarterlyTrendSection`;
- теперь блок `Динамика загрузки по кварталам` показывается только на вкладке `ТехБлок`;
- на вкладке `ТехБлок` блок рендерится в существующий контейнер `#quarterly-trend-section` внутри блока `Загрузка центра`, ниже уже существующих блоков;
- из `renderTechBlockProjectPlanFact` убрана вставка квартальной динамики, поэтому на вкладке `ПП` внутри `План-факт по проектам` этот блок больше не отображается;
- удален уже неиспользуемый helper `renderTechBlockQuarterlyTrendChart`;
- функция `renderTechBlockProjectPlanFact` больше не принимает неиспользуемый параметр `options`.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8081/`;
- во встроенном браузере проверена вкладка `ТехБлок`:
  - активна вкладка `ТехБлок`;
  - блок `Динамика загрузки по кварталам` отображается;
  - badge блока: `47% • II квартал апрель-июнь 2026`;
  - canvas `#quarterly-trend-chart` создан;
  - блок расположен внутри `Загрузка центра`;
- во встроенном браузере проверена вкладка `ПП`:
  - внутри `#production-program-content` квартальных карточек нет;
  - блок `План-факт по проектам` остался на месте;
- ошибок консоли не было.

## 39. Галочки показа факта в блоках производственной программы

Дата фиксации: 11.06.2026 14:19 MSK

Пользователь попросила:

```text
теперь для каждого блока Общая загрузка по месяцам, Проекты из плана: сроки и часы по центрам, Сверка плана продаж и фактического выполнения - добавь галочку, по которому будет отображаться и скрываться факт. по умолчанию Факт должен быть скрыт и отображаться только план
```

Было сделано:

- через Context7 проверен подход с checkbox-toggle и управлением видимостью через CSS-классы;
- добавлен общий компонент `renderFactToggle`;
- добавлен обработчик `handleFactToggleChange`;
- в `DOMContentLoaded` добавлен делегированный обработчик `change` для `.fact-toggle__input`;
- для блоков добавлен класс `fact-hidden` по умолчанию и атрибут `data-fact-toggle-block`;
- фактические элементы помечены атрибутом `data-fact-field`;
- в `style.css` добавлено правило:

```css
[data-fact-toggle-block].fact-hidden [data-fact-field] {
    display: none !important;
}
```

Изменено по блокам:

- `Общая загрузка по месяцам`:
  - скрываются `Факт часов`, `В работе`, badge текущего факта и строка шкалы `в работе`;
  - остается фонд часов и `Прогноз / очередь`;
  - подпись шкалы в скрытом режиме показывает только `будущая потребность`;
- `Проекты из плана: сроки и часы по центрам`:
  - скрываются фактические часы, исполнение плана, перегрузка, факт-легенда, факт-строка шкалы, колонки `Факт`, `Отклонение`, `Загрузка`;
  - сетка перестраивается под плановые колонки;
  - остается плановый период, плановые часы, количество проектов в плане и плановые сроки по центрам;
- `Сверка плана продаж и фактического выполнения`:
  - по умолчанию заголовок отображается как `Сверка плана продаж`;
  - скрываются карточки факта, легенда факта, фактическая линия, этапы, статус, отклонение и фактические часы в примечаниях;
  - сетка перестраивается под плановые колонки.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8082/`;
- во встроенном браузере проверена вкладка `ПП`;
- в дефолтном состоянии все три checkbox выключены;
- в дефолтном состоянии у всех трех блоков `visibleFactFields = 0`;
- после включения галочек факт появляется отдельно в каждом блоке;
- ошибок консоли не было.

## 40. `Динамика загрузки по кварталам` на `ТехБлок` из плоских таблиц

Дата фиксации: 11.06.2026 14:50 MSK

Пользователь попросила:

```text
динамику загрузки на вкладке ТехБлок считай тоже по плоским таблицам
```

Было сделано:

- через Context7 проверен подход к группировке записей по кварталам через `Map`;
- старый расчет `getQuarterlyTrendData` сохранен для прежнего клеточного источника;
- общий квартальный агрегатор вынесен в `buildQuarterlyTrendData`;
- добавлен новый расчет `getTechBlockQuarterlyTrendDataFromTables`;
- для `ТехБлок` график теперь использует именно `getTechBlockQuarterlyTrendDataFromTables`;
- факт для квартальной динамики берется из `Загрузка_ТехБлок`:
  - строки с пустым `ID` исключаются;
  - строки с `Прогноз` исключаются;
  - административный персонал исключается;
  - отсутствия, кроме командировки `к`, исключаются;
  - строки после даты увольнения исключаются;
  - будущие месяцы после текущего месяца справочника исключаются;
- фонд часов берется из плоских таблиц через уже существующий `getTechBlockCenterCapacityByMonth`:
  - `Сотрудники_ТехБлок`;
  - `Табель`;
  - отсутствия из `Загрузка_ТехБлок`;
- добавлен helper `parseMonthKeyToDate`;
- подзаголовок блока изменен на:

```text
Процент считается по плоским таблицам: фактические часы из «Загрузка_ТехБлок» к фонду часов из сотрудников и табеля.
```

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8083/`;
- во встроенном браузере проверена вкладка `ТехБлок`;
- блок `Динамика загрузки по кварталам` отображается;
- подзаголовок показывает новый источник из плоских таблиц;
- badge изменился со старого клеточного расчета `47% • II квартал апрель-июнь 2026` на `67% • II квартал апрель-июнь 2026`;
- ошибок консоли не было.

## 41. Факт по умолчанию на вкладках кроме `ПП`

Дата фиксации: 11.06.2026 15:09 MSK

Пользователь попросила:

```text
на всех остальных вкладках, кроме ПП выводи факт по умолчанию
```

Было сделано:

- через Context7 проверен подход с default-checked checkbox;
- `renderFactToggle` получил параметр `checked = false`;
- для блока `Общая загрузка по месяцам` на вкладках центров:
  - убран стартовый класс `fact-hidden`;
  - checkbox `Факт` рендерится как `checked`;
  - фактические KPI, badge и строка `в работе` видны сразу;
- блоки на вкладке `ПП` не изменены:
  - `Общая загрузка по месяцам`;
  - `Проекты из плана: сроки и часы по центрам`;
  - `Сверка плана продаж`;
  - факт там по-прежнему скрыт по умолчанию.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8084/`;
- во встроенном браузере проверена вкладка `ЦНГО`:
  - блок `Общая загрузка по месяцам` существует;
  - checkbox `Факт` включен;
  - `fact-hidden = false`;
  - `visibleFactFields = 6`;
- во встроенном браузере проверена вкладка `ПП`:
  - у трех блоков checkbox `Факт` выключен;
  - `fact-hidden = true`;
  - `visibleFactFields = 0`;
- ошибок консоли не было.

## 42. Удаление вкладки `Расчеты` из навигации

Дата фиксации: 11.06.2026 15:25 MSK

Пользователь попросила:

```text
убери вкладку Расчеты
```

Было сделано:

- через Context7 проверена роль `button` как элемента навигационного управления;
- из `index.php` удалена nav-кнопка:

```html
<button class="nav-btn" data-sheet="Расчеты" title="Самопроверка расчетов">Расчеты</button>
```

- внутренний контейнер и функции расчетов оставлены неиспользуемыми, чтобы не затрагивать связанную диагностическую логику.

Проверка:

- `node --check script.js` прошел успешно;
- `php -l index.php` не выполнен, потому что `php` не установлен в текущем окружении;
- локальный сервер запущен на `http://127.0.0.1:8085/`;
- во встроенном браузере проверена навигация:
  - `Расчеты` отсутствует;
  - порядок вкладок: `ТехБлок`, `ЦНГО`, `ЦДЦМ`, `ЦДПМ`, `ЦОТ`, `УВП`, `ЛОИ`, `НК/ТК`, `ПП`;
  - активная вкладка по умолчанию `ТехБлок`;
- ошибок консоли не было.

## 43. Подписи шкалы `Общая загрузка` без переноса

Дата фиксации: 11.06.2026 15:36 MSK

Пользователь попросила:

```text
сделай без переноса название шкалы
```

Было сделано:

- через Context7 проверен CSS-подход `white-space: nowrap`;
- в `style.css` для `.center-monthly-load-layer` ширина колонки подписи увеличена с `92px` до `118px`;
- для `.center-monthly-load-layer__label` добавлено:

```css
white-space: nowrap;
```

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8086/`;
- во встроенном браузере проверена вкладка `ПП`:
  - `прогноз / очередь` отображается одной строкой;
  - `white-space = nowrap`;
  - высота подписи равна одной строке;
- во встроенном браузере проверена вкладка `ЦНГО`:
  - `в работе` отображается одной строкой;
  - `прогноз / очередь` отображается одной строкой;
  - обе подписи имеют `white-space = nowrap`;
- ошибок консоли не было.

## 44. Использование столбца `Месяц` на листе `План_продаж`

Дата фиксации: 11.06.2026 15:48 MSK

Пользователь попросила:

```text
в эксель на вкладку План_продаж добавлен 5 столбец месяц - используй его, чтобы понимать загрузку по часам не просто за год, а по месяцам
```

Было сделано:

- через Context7 сверена работа с `Map` для накопления значений по ключам месяцев;
- через Python/openpyxl проверена структура листа `План_продаж`: 5-й столбец называется `Месяц`, значения представлены датами вида `2026-05-01`;
- в `script.js` добавлена поддержка дат формата `YYYY-MM-DD HH:MM:SS` в `parseDateValue`;
- `getTechBlockSalesPlanByProject()` теперь:
  - читает `План_продаж[Месяц]`;
  - сохраняет помесячную карту `planHoursByMonth`;
  - учитывает месяц в ключе дедупликации строк, чтобы разные месяцы одного проекта не схлопывались;
- добавлены helpers:
  - `getSalesPlanMonthKey`;
  - `parseSalesPlanMonthDate`;
  - `getSalesPlanMonthFallbackYear`;
  - `getSalesPlanMonthHoursByCenterForProject`;
  - `getPlanProjectHoursByMonth`;
- для проектов из `План_продаж` месячные плановые часы теперь строятся из `planHoursByMonth`, а равномерное распределение по датам проекта осталось fallback-логикой;
- в верхнем блоке `Общая загрузка по месяцам` на вкладке `ПП` видимый ряд при скрытом факте переключен на план и подписан как `план`.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8087/`;
- во встроенном браузере проверена вкладка `ПП`:
  - блок `Общая загрузка по месяцам` имеет класс `fact-hidden`;
  - ряд факта скрыт;
  - видимый ряд подписан `план`;
  - месячные значения плана отображаются по месяцам из `План_продаж`: май, июнь, июль 2026;
  - KPI показывает план за текущий месяц: `План 178% июнь • 60 641 ч`;
- ошибок консоли не было.

## 45. Новая структура `План_продаж` и фильтры вкладки `ПП`

Дата фиксации: 15.06.2026 09:54 MSK

Пользователь попросила:

```text
в экселе изменилась таблица на листе "План_продаж", внеси изменения, согласно картинке и добавь на вкладку ПП возможность фильтровать проекты по столбцам "Бизнес-направление", "Статус проекта" и "Место возникновения затрат (МВЗ)" и "Наименование конструкторского бюро". столбец управление нужен для связки проектов из плана продаж и Проектов_техблок, чтобы понимать к какому управлению или центру относится
```

Дополнительное уточнение:

```text
связь таблицы План_прожад и Проекты_техблок не только по ИД проекта, но и по названию управления или центра (в плане продаж это один столбец Управление, в нем и управления и центры записаны)
```

Было сделано:

- через Context7 сверена работа с `Set`/`Map` для уникальных опций фильтров и агрегации строк;
- через Spreadsheet skill и openpyxl проверены фактические заголовки листа `План_продаж`:
  - `Номер задачи`;
  - `Бизнес-направление`;
  - `Статус проекта`;
  - `Наименование проекта`;
  - `Дата начала проекта`;
  - `Дата окончания проекта`;
  - `Место возникновения затрат (МВЗ)`;
  - `Наименование конструкторского бюро`;
  - `Управление`;
  - `Месяц`;
  - `Часы`;
- `getTechBlockSalesPlanByProject()` переведен на новые колонки:
  - ID проекта читается из `Номер задачи`;
  - плановые часы читаются из `Часы`;
  - месяц по-прежнему читается из `Месяц`;
  - фильтровые поля сохраняются в объекте проекта;
- добавлена связка `План_продаж[Управление] -> центр`:
  - если в `Управление` указан центр, он распознается напрямую;
  - если указано управление, центр ищется по `Проекты_ТехБлок`;
  - резервно lookup дополняется по `Сотрудники_ТехБлок`;
- плановые часы теперь агрегируются не только по проекту и месяцу, но и по центру из `Управление`;
- плановые проекты из `План_продаж`, которых нет в `Проекты_ТехБлок`, добавляются на вкладку `ПП` как плановые строки;
- на вкладку `ПП` добавлена панель фильтров:
  - `Бизнес-направление`;
  - `Статус проекта`;
  - `МВЗ`;
  - `Конструкторское бюро`;
  - кнопка `Сбросить`;
- фильтры сразу перерисовывают все блоки `ПП`: месячную загрузку, сроки/часы по центрам и сверку по проектам;
- добавлены стили и адаптивность панели фильтров для desktop/tablet/mobile.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8088/`;
- во встроенном браузере проверена вкладка `ПП`:
  - отображаются 4 фильтра;
  - `Бизнес-направление` содержит `Б`, `Н`;
  - `Статус проекта` содержит `Вероятная`, `Подтвержден`, `Прогнозный`;
  - `МВЗ` в текущей книге пустой, поэтому в фильтре только `Все`;
  - `Конструкторское бюро` содержит 3 значения из плана продаж;
  - при выборе `Бизнес-направление = Б` список сокращается с 396 до 36 проектов;
  - кнопка `Сбросить` возвращает 396 проектов;
  - плановая шкала `ПП` показывает месяцы май-июль 2026;
  - ошибок консоли не было.

## 46. Перенос чекбокса `Факт` в панель фильтров `ПП`

Дата фиксации: 15.06.2026 10:23 MSK

Пользователь попросила:

```text
убери эту надпись, перемести "факт" на это место
```

Было сделано:

- через Context7 сверена DOM-логика переключения классов через checkbox;
- из панели фильтров вкладки `ПП` убрана надпись вида `Показано 396 из 396 проектов`;
- на место этой надписи добавлен единый чекбокс `Факт`;
- внутренние чекбоксы `Факт` из блоков `ПП` удалены:
  - `Общая загрузка по месяцам`;
  - `Проекты из плана: сроки и часы по центрам`;
  - `Сверка по каждому проекту`;
- новый верхний чекбокс управляет видимостью фактических полей сразу во всех блоках `ПП`;
- состояние чекбокса сохраняется при перерисовке после изменения фильтров.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере проверена вкладка `ПП` на `http://127.0.0.1:8088/`:
  - текст `Показано...` в панели фильтров отсутствует;
  - в панели фильтров есть один чекбокс `Факт`;
  - локальных чекбоксов `Факт` внутри блоков нет;
  - по умолчанию факт скрыт во всех 3 блоках;
  - при включении верхнего `Факт` все 3 блока показывают фактические поля;
  - ошибок консоли не было.

## 47. Единый стиль блока `Общая загрузка по месяцам` на `ПП`

Дата фиксации: 15.06.2026 10:27 MSK

Пользователь попросила:

```text
сделай в едином стиле блоки, чтобы как на нижнем была рамка и не был такой отступ большой по бокам у блока "Общая загрузка по месяцам"
```

Было сделано:

- через Context7 сверены CSS-подходы к `border`, `padding` и `box-sizing`;
- для `.tech-monthly-load-card` добавлены:
  - рамка `1px solid var(--accent-neutral-border)`;
  - `border-radius: 8px`;
  - белый фон без градиента;
  - отключение тени;
  - компактный `padding: 16px`;
- у шапки блока добавлена нижняя разделительная линия, как у нижнего блока;
- внутренний блок шкалы сделан компактнее по padding.

Проверка:

- во встроенном браузере проверена вкладка `ПП` на `http://127.0.0.1:8088/`;
- у `Общая загрузка по месяцам`:
  - рамка совпадает с нижним блоком;
  - радиус 8px;
  - белый фон;
  - тень отсутствует;
  - боковые отступы уменьшены;
- ошибок консоли не было.

## 48. Ускорение переключения вкладок на реальном файле

Дата фиксации: 15.06.2026 16:31 MSK

Пользователь заметила:

```text
долго загружаются вкладки, когда загружаешь реальный файл с данными
```

Было сделано:

- через Context7 сверена стратегия кэширования дорогих вычислений через `Map`;
- добавлен общий `workbookDataCache`, который очищается при каждой новой загрузке Excel;
- закэшировано чтение строк листов через `getWorksheetRowsByHeaders()`;
- закэширован `createWorksheetCache()` по имени листа, чтобы при переключении вкладок не сканировать большой лист заново;
- закэшированы тяжёлые агрегаты:
  - сотрудники техблока;
  - загрузка по проектам;
  - табель;
  - план-факт по проектам;
  - фонд часов по диапазонам месяцев;
- для вкладки `ПП` добавлен кэш готовой HTML-разметки по текущим фильтрам, состоянию `Факт` и раскрытию деталей;
- блок `Сверка по каждому проекту` на `ПП` переведён на ленивую отрисовку:
  - в закрытом состоянии 396 строк не создаются;
  - строки строятся только при раскрытии блока;
  - при закрытии блок снова разгружает DOM.

Проверка:

- `node --check script.js` прошёл успешно;
- локальный сервер запущен на `http://127.0.0.1:8089/`;
- во встроенном браузере проверена вкладка `ПП`:
  - фильтры отображаются;
  - в закрытом блоке `Сверка по каждому проекту` строк `.planfact-row` нет;
  - вместо строк отображается лёгкая заглушка;
  - при раскрытии блока строки проектов строятся и отображаются;
  - ошибок консоли не было.

Примечание:

- браузерный automation-замер клика остаётся около 2 секунд и, похоже, включает накладные расходы самого инструмента проверки; при этом реальные повторные чтения Excel-листов и немедленная отрисовка сотен строк из кода убраны.

## 49. Пояснение расчёта процента `в работе` в блоке `Общая загрузка по месяцам`

Дата фиксации: 17.06.2026 10:10 MSK

Пользователь спросила:

```text
как ты считаешь процент загрузки проектов, которые в работе, в блоке Общая загрузка по месяцам, у каждого центра?
```

Было проверено:

- через Context7 сверена базовая логика агрегации по ключам через `Map` и округления процента;
- в `script.js` найдены функции:
  - `renderCenterMonthlyLoadSection()`;
  - `getActiveProjectActualHoursByMonthForCenter()`;
  - `getMonthlyProjectLoadRows()`;
  - `getTechBlockLoadByProject()`;
  - `getTechBlockCenterCapacityByMonth()`.

Текущая формула:

- `в работе % = round(фактические часы активных проектов центра за месяц / фонд часов центра за месяц * 100)`;
- фактические часы берутся из `Загрузка_ТехБлок` по строкам без прогноза, без отсутствий, без административного персонала и только по активным на дату сотрудникам;
- в числитель попадают только проекты со статусом `active` / `В работе`;
- фонд часов для плоских расчётов строится из `Сотрудники_ТехБлок` и `Табель` с вычитанием отсутствий.

## 50. Файл передачи реализации для другого ИИ

Дата фиксации: 23.06.2026 14:47 MSK

Пользователь попросила:

```text
напиши или обнови файл, где будет подробно написано, что было реализовано и как, чтобы данные дать почитать другой ИИ и она могла к себе внедрить проделанные действия
```

Было сделано:

- через Context7 сверены базовые подходы к читаемой Markdown-документации;
- создан файл `Передача_реализации_для_ИИ.md`;
- в файл вынесено подробное описание:
  - текущего состояния проекта;
  - порядка вкладок;
  - вкладки `ПП` как отдельного режима, а не листа Excel;
  - структуры листов `План_продаж`, `Проекты_ТехБлок`, `Загрузка_ТехБлок`, `Сотрудники_ТехБлок`, `Табель`;
  - связи `План_продаж` с проектами и центрами;
  - расчета план-факта;
  - месячного распределения плановых часов по `План_продаж[Месяц]`;
  - расчета факта, прогноза и фонда часов;
  - блока `Общая загрузка по месяцам` на `ПП` и на вкладках центров;
  - квартальной динамики;
  - фильтров и чекбокса `Факт` на `ПП`;
  - удаленных блоков;
  - кэширования и ленивого рендера;
  - проверок после внедрения;
  - карты ключевых функций;
  - рекомендуемого порядка внедрения в другой проект.

Проверка:

- файл создан в корне проекта;
- проверено начало и окончание файла;
- объем файла: `866` строк;
- попытка `git status --short` показала, что текущая папка не является git-репозиторием.

## 51. Целевой показатель под блоком `Уведомления по проектам`

Дата фиксации: 29.06.2026 11:32 MSK

Пользователь попросила:

```text
на дашборд нужно добавить вычисление Целевого показателя и разместить его под блоком Уведомления по проектам
```

Дополнительные уточнения:

```text
задачи могут повторяться, поэтому нужно брать по датам, если начало, минимальную, если завершение - максимальную, если часы, то сумму. если статус где-то завершен, где-то нет, то общий - в работе
```

```text
Мобщ - это тоже завершенные задачи, а не все и мы смотрим сколько из завершенных задач выполнено в срок или по часам
```

Было сделано:

- через Context7 сверена DOM-логика обновления блоков через `innerHTML`, `classList` и управление видимостью;
- в `index.php` под `notification-block` добавлен новый контейнер:
  - `target-indicator-block`;
  - `target-indicator-content`;
- в `script.js` добавлены функции:
  - `updateTargetIndicator()`;
  - `getTargetIndicatorData()`;
  - `getOrderedTargetIndicatorRows()`;
  - `calculateTargetIndicatorPercent()`;
  - `isTargetIndicatorProjectCompleted()`;
  - `renderTargetIndicatorContent()`;
  - `renderTargetIndicatorMetric()`;
  - `formatTargetIndicatorPercent()`;
- `updateTargetIndicator()` подключен в общий `renderAll()` сразу после `updateNotification()`;
- расчет идет по листу `Проекты_ТехБлок`:
  - `Центр`;
  - `ID`;
  - `Вид проекта`;
  - `Статус`;
  - `Дата завершения работ`;
  - `Плановые часы`;
  - `Фактические часы`;
  - `ФАКТ начало`;
  - `ФАКТ окончание`;
- повторяющиеся строки задачи группируются по паре `центр + ID`;
- для повторяющихся задач применены правила:
  - `ФАКТ начало` = минимальная дата;
  - `ФАКТ окончание` = максимальная дата;
  - `Дата завершения работ` = максимальная дата;
  - плановые и фактические часы суммируются;
  - задача считается завершенной только если все строки группы имеют статус `Завершена`; если хотя бы одна строка не завершена, общий статус считается не завершенным / в работе;
- в расчет берутся задачи, у которых минимальная фактическая дата начала попадает в `2026`;
- знаменатель для процентов теперь завершенные задачи, а не все начатые:
  - `В срок % = задачи в срок / завершенные задачи * 100`;
  - `В норме часов % = задачи в пределах плановых часов / завершенные задачи * 100`;
- на вкладке `ТехБлок` блок показывает таблицу по всем центрам;
- на вкладках центров блок показывает один центр с карточками:
  - `Начато`;
  - `Завершено`;
  - `В срок`;
  - `В норме часов`;
- в `style.css` добавлены стили для блока целевого показателя, таблицы и карточек метрик.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен на `http://127.0.0.1:8080/`;
- во встроенном браузере проверена вкладка `ЦНГО`:
  - блок отображается под уведомлениями;
  - после исправления знаменателя показывает `В срок 48%`, `11 из 23`;
  - `В норме часов 48%`, `11 из 23`;
- во встроенном браузере проверена вкладка `ТехБлок`:
  - блок показывает сводную таблицу по 6 центрам;
  - заголовки таблицы: `Центр`, `Нач.`, `Зав.`, `Срок`, `Часы`;
  - проценты считаются от завершенных задач;
- ошибок и предупреждений в консоли браузера не было.

## 52. Запуск сайта во внешнем браузере

Дата фиксации: 29.06.2026 11:35 MSK

Пользователь попросила:

```text
запусти сайт во внешнем браузере, чтобы проверить
```

Было сделано:

- через Context7 сверена базовая логика локального Node.js HTTP-сервера;
- локальный сервер запущен командой `node local-server.mjs`;
- сайт открыт во внешнем браузере macOS по адресу `http://127.0.0.1:8080`;
- доступность страницы проверена через `curl -I http://127.0.0.1:8080`.

Проверка:

- сервер ответил `HTTP/1.1 200 OK`;
- `Content-Type: text/html; charset=UTF-8`;
- сервер оставлен запущенным, чтобы пользователь могла проверить сайт во внешнем браузере.

## 53. Целевой показатель только по коммерческим и инвестиционным проектам

Дата фиксации: 29.06.2026 11:39 MSK

Пользователь попросила:

```text
подкорректируй расчет, что рассматриваются только коммерческие проекты и инвестиционные
```

Было сделано:

- через Context7 сверена логика фильтрации массива через `some()`/`filter()` и нормализованный текст;
- в расчете целевого показателя добавлен учет `Вид проекта`;
- повторяющиеся строки задачи теперь собирают все значения `Вид проекта` в `projectTypes`;
- перед попаданием задачи в расчет добавлен фильтр `isTargetIndicatorProjectTypeIncluded()`:
  - допускаются значения, где нормализованный текст содержит `коммерческ`;
  - допускаются значения, где нормализованный текст содержит `инвестицион`;
  - остальные виды проектов исключаются из `Начато`, `Завершено`, `В срок`, `В норме часов`;
- подписи блока уточнены:
  - на `ТехБлок`: `коммерческие и инвестиционные задачи`;
  - на вкладках центров: `коммерческие и инвестиционные проекты центра`.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере обновлена вкладка `ТехБлок`:
  - блок показывает сводку только по коммерческим и инвестиционным задачам;
  - пример после фильтра: `ЦНГО` — `Нач. 35`, `Зав. 6`, `Срок 50%`, `Часы 67%`;
- во встроенном браузере проверена вкладка `ЦНГО`:
  - `Начато 35`;
  - `Завершено 6`;
  - `В срок 50%`, `3 из 6`;
  - `В норме часов 67%`, `4 из 6`;
- ошибок и предупреждений в консоли браузера не было.

## 54. Фильтр проектов по участию в расчете целевого показателя

Дата фиксации: 29.06.2026 11:52 MSK

Пользователь попросила:

```text
можешь в таблице проектов каждого центра добавить фильтр, который отобразит только те проекты, которые использовались для расчета целевого показателя?
```

Было сделано:

- через Context7 сверена логика `Set.has`, `Array.filter` и обработчика `change` для checkbox;
- добавлено состояние `projectsTargetIndicatorOnly`;
- в блок `Проекты` на вкладках центров добавлен checkbox:
  - `Показать только проекты из расчета целевого показателя`;
- расчет целевого показателя и фильтр таблицы проектов теперь используют общую базу проектов:
  - `getTargetIndicatorProjectRecords()`;
  - `getTargetIndicatorProjectKey()`;
  - `isTargetIndicatorProjectStartedInYear()`;
  - `isTargetIndicatorProjectCompletedInYear()`;
  - `getTargetIndicatorProjectKeySet()`;
- фильтр таблицы использует проекты из знаменателя целевого показателя:
  - коммерческие или инвестиционные;
  - фактическое начало в `2026`;
  - фактическое окончание в `2026`;
  - все повторяющиеся строки задачи имеют статус `Завершена`;
- `getProjectsFromDetails()` теперь принимает `targetProjectKeys` и отфильтровывает строки деталей по ключу `центр + ID`;
- при включенном фильтре числа в матрице проектов пересчитываются по отфильтрованным строкам, а не берутся из исходной сводки Excel;
- подпись checkbox при включенном фильтре показывает количество проектов, реально отображенных в текущей таблице центра.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере проверена вкладка `ЦНГО`;
- в обычном режиме таблица показывает исходные значения;
- после включения фильтра отображается подпись `Показаны проекты из расчета целевого показателя: 4`;
- в матрице осталась одна раскрываемая ячейка:
  - `Завершена / Коммерческий проект`;
  - счетчик `4`;
- при раскрытии ячейки отображается список из 4 проектов;
- ошибок и предупреждений в консоли браузера не было.

## 55. ID задач из расчета целевого показателя

Дата фиксации: 29.06.2026 12:03 MSK

Пользователь попросила:

```text
выведи ИД задач, которые используются для расчета из листа Проекты_Техблок. укажи в блоке ниже, под целевыми показателями
```

Было сделано:

- через Context7 сверена структура `details/summary`, но для более прямого отображения выбран видимый список ID-чипов;
- в блок `Целевой показатель` добавлена секция `ID задач в расчете`;
- ID берутся из той же базы `Проекты_ТехБлок`, что используется для знаменателя целевого показателя:
  - коммерческие или инвестиционные проекты;
  - фактическое начало в `2026`;
  - фактическое окончание в `2026`;
  - все повторяющиеся строки задачи имеют статус `Завершена`;
  - группировка по `центр + ID`;
- на вкладках центров выводится список ID текущего центра;
- на вкладке `ТехБлок` ID группируются по центрам;
- для длинных списков добавлено ограничение высоты и внутренний скролл;
- в `style.css` добавлены стили для блока `target-indicator-task-ids` и ID-чипов.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере проверена вкладка `ЦНГО`:
  - под целевыми показателями отображается `ID задач в расчете`;
  - показано 6 ID: `10595`, `17327`, `27122`, `27123`, `28575`, `29689`;
  - количество ID совпадает с `Завершено 6`;
- во встроенном браузере проверена вкладка `ТехБлок`:
  - ID сгруппированы по центрам;
  - `ЦНГО` — 6 ID;
  - `ЦДЦМ` — 6 ID;
  - `ЦДПМ` — 3 ID;
  - `УВП` — 8 ID;
  - `ЛОИ` — 160 ID;
  - центр с нулем завершенных задач не выводится в списке ID;
- ошибок и предупреждений в консоли браузера не было.

## 56. Проверка ограничения вывода проектов из диапазона `AM:BA`

Дата фиксации: 29.06.2026 12:08 MSK

Пользователь спросила:

```text
есть ли ограничение на вывод проектов для таблицы проектов из ячеек AM:BA?
```

Было проверено:

- через Context7 сверена логика `while`/`break` для ограничения чтения строк;
- в `script.js` найден конфиг `EXCEL_CONFIG.projects.detailsTable`;
- диапазон детальной таблицы задан как `AM:BA`, стартовая строка `5`;
- фактически для таблицы проектов используются колонки:
  - `AN` — ID;
  - `AO` — управление/центр;
  - `AP` — название;
  - `AQ` — вид проекта;
  - `AR` — статус;
  - `AS` — плановая дата;
  - `AT` — прогнозируемая дата;
  - `AU` — плановые часы;
  - `AV` — фактические часы;
  - `AW` — цвет индикатора по дате;
  - `AX` — текст индикатора по дате;
  - `AY` — цвет индикатора по часам;
  - `AZ` — процент остатка по часам;
- в функции `getProjectsFromDetails()` найдено ограничение `maxRows = 200`;
- цикл чтения начинается со строки `5`;
- чтение прекращается при первом пустом ID в колонке `AN`.

Вывод:

- да, ограничение есть: максимум `200` строк начиная с `5` строки;
- дополнительно список обрывается раньше, если в колонке `AN` встречается пустой ID.

## 57. Увеличение чтения таблицы проектов до последней заполненной строки `AM`

Дата фиксации: 29.06.2026 12:11 MSK

Пользователь попросила:

```text
увеличь лимит до последней заполненной строки в столбце AM
```

Было сделано:

- через Context7 сверена базовая логика `for`-прохода по диапазону строк;
- в `getProjectsFromDetails()` удален жесткий лимит `maxRows = 200`;
- добавлена функция `getLastFilledProjectDetailsRow(config)`;
- последняя строка детальной таблицы теперь определяется по последней заполненной ячейке в колонке `AM`;
- чтение деталей проектов теперь идет от `startRow` до найденной последней строки `AM`;
- пустой `ID` в колонке `AN` больше не обрывает чтение всего списка, а пропускает только текущую строку.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере обновлена вкладка `ЦНГО`;
- таблица проектов отрисовалась;
- фильтр проектов по целевому показателю остался доступен;
- ошибок и предупреждений в консоли браузера не было.

## 58. Сворачивание списка ID задач в целевом показателе

Дата фиксации: 29.06.2026 12:23 MSK

Пользователь попросила:

```text
сделай блок с перечислением ИД скрытым, но с возможностью раскрыть
```

Было сделано:

- через Context7 сверена работа HTML-элементов `details` и `summary`;
- секция `ID задач в расчете` в блоке `Целевой показатель` переведена на `details`;
- список ID теперь закрыт по умолчанию;
- в закрытом состоянии виден только заголовок `ID задач в расчете` и количество задач;
- по клику на заголовок список ID раскрывается;
- стили `target-indicator-task-ids__title` обновлены под интерактивный заголовок.

Проверка:

- `node --check script.js` прошел успешно;
- во встроенном браузере проверена вкладка `ЦНГО`;
- в закрытом состоянии `details.open = false`, виден только заголовок и число `6`;
- после клика `details.open = true`, отображаются ID:
  - `10595`;
  - `17327`;
  - `27122`;
  - `27123`;
  - `28575`;
  - `29689`;
- ошибок и предупреждений в консоли браузера не было.

## 59. Фильтрация статусов в таблице проектов

Дата фиксации: 29.06.2026 14:18 MSK

Пользователь уточнила:

```text
оставляем статусы В работе, на паузе, в очереди, у заказчика, завершена, все остальное - не выводим
```

Было сделано:

- через Context7 сверена базовая JS-логика фильтрации массивов через `Array.prototype.filter()`;
- в `script.js` добавлены единые константы для разрешенных статусов таблицы проектов:
  - `В работе`;
  - `На паузе`;
  - `В очереди`;
  - `У заказчика`;
  - `Завершена`;
- после агрегации проектов из листа `Проекты_ТехБлок` добавлен отсев всех проектов, чей итоговый статус не входит в разрешенный набор;
- порядок строк таблицы теперь строится по тому же списку разрешенных статусов;
- статусы вроде `Отменена`, `Потенциальная`, `-` и любые другие больше не попадают ни в матрицу количества, ни в раскрывающиеся списки проектов.

Проверка:

- `node --check script.js` прошел успешно.

## 60. Текст отставания по сроку в таблице проектов

Дата фиксации: 29.06.2026 14:21 MSK

Пользователь попросила:

```text
вместо "Риск срока" выводи "отставание на X дней", где X= дата завершения работ по договору-прогноз окончания
```

Было сделано:

- через Context7 сверена работа `Date.getTime()` для расчета разницы между датами;
- в `script.js` обновлена функция `getProjectTableDateIndicator()`;
- если прогноз окончания не позже даты завершения работ по договору, текст остается `Окончание в срок`;
- если прогноз окончания позже договорной даты, вместо `Риск срока` выводится текст вида `отставание на 5 дней`;
- для X используется положительное количество дней просрочки: `прогноз окончания - дата завершения работ по договору`;
- добавлена функция `getRussianDayWord()` для корректного склонения:
  - `1 день`;
  - `2 дня`;
  - `5 дней`.

Проверка:

- `node --check script.js` прошел успешно;
- мини-проверка расчета в Node показала:
  - `отставание на 5 дней`;
  - `1 день, 2 дня, 5 дней, 11 дней, 21 день, 24 дня`.

## 61. Факт в `Общая загрузка по месяцам` по всем статусам со списанными часами

Дата фиксации: 29.06.2026 14:25 MSK

Пользователь попросила:

```text
а теперь для блока Общая загрузка по месяцам на каждой вкладке центра нужно считать проекты не только, которые в работе, а с любым статусом, у кого были списаны часы в определенном месяце
```

Было сделано:

- через Context7 сверена базовая логика фильтрации массивов через `Array.prototype.filter()`;
- в `renderCenterMonthlyLoadSection()` заменен источник фактических часов:
  - было: `getActiveProjectActualHoursByMonthForCenter()`, только проекты со статусом `В работе`;
  - стало: `getActualProjectHoursByMonthForCenter()`, все проекты, у которых есть фактические списания в месяце;
- для центра факт берется из `actualHoursByCenterMonth`, который наполняется из листа `Загрузка_ТехБлок` по строкам с датой и часами;
- фильтр по статусу проекта для факта на вкладках центров удален;
- прогноз/очередь не менялись и продолжают считаться прежними правилами;
- подписи блока на вкладках центров уточнены:
  - `списанные часы / фонд часов`;
  - KPI `Факт`;
  - шкала `факт`;
  - подзаголовок `факт и будущая потребность`.

Проверка:

- `node --check script.js` прошел успешно;
- проверены строки формирования `actualHoursByCenterMonth`: месяцы факта строятся из `Загрузка_ТехБлок`, а не из статуса проекта.

## 62. Исправление майского факта в `Общая загрузка по месяцам`

Дата фиксации: 29.06.2026 14:34 MSK

Пользователь указала расхождение:

```text
в мае месяце кол-во фактических часов было 3356, а в блоке Общая загрузка по месяцам он пишет 1867 ч
```

Что было найдено:

- через Context7 сверена базовая работа `Array.prototype.filter()`;
- по реальному файлу `Dashboard_report_Technical_unit.xlsm` проверен лист `Загрузка_ТехБлок`;
- для `ЦНГО` за май 2026 сырая сумма строк с часами составляет `3356.0`;
- из этих часов:
  - `2058.25` попадали в прежнюю логику как обычные фактические строки;
  - `1297.75` имели флаг `Прогноз = 1`;
- так как май 2026 уже не будущий месяц, эти строки с `Прогноз = 1` должны попадать в факт блока месячной загрузки, а не пропадать.

Было сделано:

- для блока `Общая загрузка по месяцам` на вкладках центров добавлена отдельная функция `getSpentProjectHoursByMonthForCenter(centerKey)`;
- блок теперь считает факт напрямую из листа `Загрузка_ТехБлок`, а не через агрегированный список проектов;
- в факт включаются все строки центра с часами за месяц, кроме:
  - административного персонала;
  - сотрудников после даты увольнения;
  - строк отсутствий;
  - прогнозных строк будущих месяцев;
- прогнозные строки прошедших и текущего месяца теперь попадают в факт, чтобы май 2026 считался как `3356.0`;
- если в строке загрузки не указан центр, используется центр из справочника `Сотрудники_ТехБлок`, как и в основном загрузчике.

Проверка:

- `node --check script.js` прошел успешно;
- отдельная проверка по XLSM показала:
  - `ЦНГО май 2026 = 3356.0`;
  - май 2026 по центрам:
    - `ЛОИ` — `3401.85`;
    - `УВП` — `1338.25`;
    - `ЦДПМ` — `2605.5`;
    - `ЦДЦМ` — `4298.5`;
    - `ЦНГО` — `3356.0`;
    - `ЦОТ` — `4416.25`.

## 63. Доведение майского факта `ЦНГО` до `3356`

Дата фиксации: 29.06.2026 14:37 MSK

Пользователь уточнила:

```text
в ЦНГО за май выводит 3124
```

Что было найдено:

- через Context7 сверена работа `Map`/агрегации значений;
- повторная проверка реального XLSM показала:
  - полная сумма строк `Загрузка_ТехБлок` по `ЦНГО` за май 2026 = `3356.0`;
  - текущий код давал `3123.75`, отображалось как `3124`;
- разница возникала из-за двух фильтров:
  - `145.0` ч отсекаются как административный персонал по справочнику `Сотрудники_ТехБлок`;
  - `87.25` ч отсекаются по дате увольнения из справочника сотрудников;
- при этом в листе `Загрузка_ТехБлок` эти строки имеют реальные списанные часы и должны входить в показатель факта блока `Общая загрузка по месяцам`.

Было сделано:

- в `getSpentProjectHoursByMonthForCenter(centerKey)` удалено отсечение:
  - административного персонала;
  - сотрудников после даты увольнения;
- для блока `Общая загрузка по месяцам` на вкладках центров теперь факт означает сумму всех списанных часов по центру и месяцу из `Загрузка_ТехБлок`;
- исключаются только:
  - строки отсутствий;
  - прогнозные строки будущих месяцев;
- центр по-прежнему берется из строки `Загрузка_ТехБлок`, а если он пустой — из `Сотрудники_ТехБлок`.

Проверка:

- `node --check script.js` прошел успешно;
- отдельная проверка формулы по XLSM вернула `3356.0` для `ЦНГО` за май 2026.

## 64. Отключение группировки проектов по управлениям для `ЦОТ` и `ЦДПМ`

Дата фиксации: 29.06.2026 14:40 MSK

Пользователь попросила:

```text
в таблице проектов для управлений ЦОТ и ЦДПМ не группируй проекты по управлениям, в остальных группировку оставляем
```

Было сделано:

- через Context7 сверена работа `Object.keys()` для обхода групп;
- в `script.js` добавлена функция `shouldGroupProjectDetailsByManagement()`;
- для вкладок/центров `ЦОТ` и `ЦДПМ` раскрывающийся список проектов теперь выводится единым списком без заголовков управлений;
- для остальных центров прежняя группировка по управлениям сохранена;
- матрица количества проектов по статусам и видам проектов не менялась.

Проверка:

- `node --check script.js` прошел успешно.

## 65. Короткое пояснение к блоку `Общая загрузка по месяцам`

Дата фиксации: 29.06.2026 14:45 MSK

Пользователь попросила:

```text
давай добавим короткое пояснение к этому блоку, чтобы было понятно, что это вмсто фразы "Центр осевых турбомашин: списанные часы / фонд часов. Прогноз и очередь показываются со следующего месяца."
```

Было сделано:

- через Context7 сверена уместность текстового пояснения в HTML-элементе `p`;
- в `script.js` заменена подпись блока `Общая загрузка по месяцам` на универсальное пояснение:
  - `Факт — списанные часы к фонду центра. Прогноз — будущая потребность со следующего месяца.`;
- персональная фраза с названием центра больше не выводится;
- удалена ставшая лишней переменная `centerName` внутри `renderCenterMonthlyLoadSection()`.

Проверка:

- `node --check script.js` прошел успешно.

## 66. Связное описание смысла блока `Общая загрузка по месяцам`

Дата фиксации: 29.06.2026 14:48 MSK

Пользователь попросила связать в одно описание смысловые фразы:

```text
- насколько центр загружен по месяцам относительно своего доступного фонда часов.
- нагрузка центра по часам во времени.
- он показывает, где центр уже был загружен фактическими списаниями, и где впереди ожидается нагрузка относительно доступного фонда часов.
```

Было сделано:

- через Context7 сверена уместность поясняющего текста в HTML-элементе `p`;
- в `script.js` подпись блока заменена на связное описание:
  - `Показывает нагрузку центра по часам во времени: где центр уже был загружен фактическими списаниями, и где впереди ожидается нагрузка относительно доступного фонда часов.`;
- расчет, KPI и шкалы блока не менялись.

Проверка:

- `node --check script.js` прошел успешно.

## 67. Периоды текущего года и плановые часы с прогнозом в `Общая загрузка`

Дата фиксации: 29.06.2026 15:02 MSK

Пользователь попросила:

```text
для блока Общая загрузка выводи начиная с текущего года периоды, а если есть плановые часы с прогнозом, их добавь в прогноз/очередь
```

Было сделано:

- через Context7 сверена работа `Date.getFullYear()` и построение периодов от текущего года;
- в `getCenterMonthlyLoadMonths()` добавлен фильтр `record.year >= getReferenceDate().getFullYear()`;
- блок `Общая загрузка по месяцам` на вкладках центров больше не показывает месяцы до текущего года;
- в объект проекта добавлено поле `forecastEnd`, чтобы расчет мог понимать наличие прогнозной даты;
- в `getForecastProjectHoursByMonthForCenter()` плановые часы по месяцам теперь добавляются в строку `прогноз / очередь`, если у проекта есть прогнозная дата окончания;
- прежний fallback для очереди сохранен: для проектов не в работе без прогнозных строк по-прежнему используются плановые месячные часы.

Проверка:

- `node --check script.js` прошел успешно.

## 68. Правило `прогноз / очередь`: план продаж плюс внеплановый прогноз из загрузки

Дата фиксации: 29.06.2026 15:10 MSK

Пользователь уточнила:

```text
для прогноза/очередь должен быть такой расчет: берем кол-во часов по месяцам из плана продаж и только их выводим. если есть проекты, которые не указаны в плане продаж, но есть прогнозные часы в загрузке, то их тоже учитываем и в подсказке указываем сколько из плана продаж часов, а сколько прогнозных из загрузки для будущих периодов
```

Было сделано:

- через Context7 сверена работа `Map` для раздельной агрегации месячных сумм;
- `getForecastProjectHoursByMonthForCenter(centerKey)` теперь считает строку `прогноз / очередь` напрямую от плоских источников:
  - `План_продаж` — основной источник часов по месяцам;
  - `Загрузка_ТехБлок` — только прогнозные часы по проектам, ID которых нет в `План_продаж`;
- для проектов из `План_продаж` прогнозные часы из загрузки больше не добавляются, чтобы не было двойного счета;
- расчет больше не зависит от фильтрованного массива план-факта и статуса проекта;
- в `getMonthlyProjectLoadRows()` добавлены компоненты:
  - `forecastSalesPlanHours`;
  - `forecastLoadHours`;
- в tooltip ячеек строки `прогноз / очередь` добавлена расшифровка:
  - `из План_продаж ... ч`;
  - `прогноз из загрузки ... ч`;
- карточки правил расчета обновлены:
  - факт переименован в `Факт по списанным часам`;
  - правило `Прогноз / очередь` описывает новый приоритет `План_продаж` и добавление только внепланового прогноза из загрузки.

Проверка:

- `node --check script.js` прошел успешно.

## 69. Разбор `402%` в `прогноз / очередь` для `ЦНГО` за июль

Дата фиксации: 29.06.2026 15:30 MSK

Пользователь спросила:

```text
почему для ЦНГО прогноз/очередь для июля 402%?
```

Что было проверено:

- через Context7 сверена работа `Map` для агрегации месячных сумм;
- по текущему файлу `Dashboard_report_Technical_unit.xlsm` пересчитан июль 2026 для `ЦНГО` по текущему правилу строки `прогноз / очередь`;
- фонд часов `ЦНГО` за июль берется из вкладки `ЦНГО`, ячейка `N46`, и равен `8 110 ч`;
- прогноз/очередь за июль складывается из:
  - `План_продаж` — `32 185,9 ч`;
  - внеплановый прогноз из `Загрузка_ТехБлок` — `444,75 ч`;
  - итого — `32 630,65 ч`;
- процент считается как `32 630,65 / 8 110 * 100 = 402%`.

Основная причина:

- почти весь процент дает `План_продаж`, а не прогнозные строки загрузки;
- в `План_продаж` за июль по `ЦНГО` найдено 15 строк, все через управление `Управление по разработке промышленного оборудования`;
- крупнейшие строки:
  - ID `29634` — `6 560 ч`;
  - ID `30128` — `4 999 ч`;
  - ID `30127` — `4 511 ч`;
  - ID `30126` — `4 269 ч`;
  - ID `29625` — `3 410 ч`;
  - ID `29160` — `3 383 ч`.

Вывод:

- `402%` — это математический результат текущего правила: июльский план продаж по `ЦНГО` примерно в 4 раза больше доступного фонда часов за июль;
- если такой процент выглядит неверно, нужно проверять, должны ли эти строки `План_продаж` действительно относиться к `ЦНГО` и именно к июлю.

## 70. Проверка периода июльских часов `ЦНГО` в `План_продаж`

Дата фиксации: 29.06.2026 15:33 MSK

Пользователь уточнила:

```text
а все эти часы у Управление по разработке промышленного оборудования именно за июль, не за другие периоды?
```

Что было проверено:

- через Context7 сверена работа `Date.getMonth()` и `Date.getFullYear()` для определения месяца даты;
- по листу `План_продаж` проверены 15 строк управления `Управление по разработке промышленного оборудования`, которые дают `32 185,9 ч` в июле для `ЦНГО`;
- в столбце `Месяц` у этих строк стоят даты с `2026-07-01` по `2026-07-15`;
- текущий расчет месячной нагрузки использует именно столбец `Месяц`, поэтому все эти строки относятся к июлю;
- при этом даты проекта могут охватывать более широкий период:
  - например, часть строк имеет `Дата начала проекта = 2026-01-01` и `Дата окончания проекта = 2026-12-01`;
  - но при наличии столбца `Месяц` расчет берет месяц из него, а не распределяет часы по диапазону проекта.

Вывод:

- по текущим данным и текущей логике эти часы считаются июльскими;
- если это не должно быть так, нужно уточнить смысл столбца `Месяц` в `План_продаж`: это месяц распределения часов или дата/порядковая метка, которую нельзя напрямую использовать как месяц загрузки.

## 71. Отображение всех месяцев из `План_продаж` в строке `прогноз / очередь`

Дата фиксации: 29.06.2026 15:39 MSK

Пользователь спросила:

```text
в файле появились и другие месяцы в плане продаж, почему отображается только июль в прогноз / очередь?
```

Что было найдено:

- в текущем файле `Dashboard_report_Technical_unit.xlsm` на листе `План_продаж` есть часы не только за июль, но и за май, июнь, август, сентябрь и октябрь 2026;
- расчет `getForecastProjectHoursByMonthForCenter()` уже собирал эти месяцы из `План_продаж`;
- проблема была не в чтении часов, а в списке месяцев для отрисовки блока `Общая загрузка по месяцам`;
- `renderCenterMonthlyLoadSection()` строил шкалу по `getCenterMonthlyLoadMonths()`, а эта функция брала месяцы из текущей шкалы центра, где для `ЦНГО` были только май, июнь и июль;
- из-за этого прогнозные часы за август-октябрь существовали в данных, но не попадали в `getMonthlyProjectLoadRows()` и не отображались.

Что изменено в `script.js`:

- в `renderCenterMonthlyLoadSection()` базовый список месяцев теперь расширяется через `getCenterMonthlyLoadMonthsWithData()`;
- новая функция добавляет в шкалу месяцы, найденные в:
  - фактических списаниях центра;
  - `totalHoursByMonth`;
  - `salesPlanHoursByMonth`;
  - `loadForecastHoursByMonth`;
- для добавленных месяцев подтягивается фонд часов центра из `getTechBlockCenterCapacityByMonth()`;
- добавлены вспомогательные функции:
  - `collectMonthlyLoadSourceKeys()`;
  - `sortMonthlyLoadRecords()`;
- месяцы сортируются по году и номеру месяца, поэтому новые месяцы встают в шкалу в правильном порядке.

Проверка:

- через Context7 сверена работа `Date` в JavaScript: месяцы имеют нумерацию с нуля, а последний день месяца можно получить через `new Date(year, monthIndex + 1, 0)`;
- `node --check script.js` прошел успешно.

Вывод:

- показывался только июль, потому что шкала блока была ограничена месяцами, найденными в старом источнике периода центра;
- теперь строка `прогноз / очередь` должна показывать все месяцы из `План_продаж`, для которых есть часы и которые относятся к текущему году или позже.

## 72. Переход плана продаж на лист `ПП` и исключение прогнозных часов

Дата фиксации: 29.06.2026 16:03 MSK

Пользователь уточнила:

```text
- давай из расчета уберем часы, которые указаны как прогноз
- вкладка План_продаж переименована в "ПП"
- столбцы теперь следующие ...
```

Что было проверено:

- через Context7 проверен стандартный подход SheetJS к чтению книги через `workbook.SheetNames` и `workbook.Sheets[name]`;
- в текущем файле `Dashboard_report_Technical_unit.xlsm` найден лист `ПП`;
- старые листы `План_продаж` и `План-продаж` в текущей книге не найдены;
- первая строка листа `ПП` содержит новую схему заголовков, включая:
  - `Бизнес-направление`;
  - `Статус проекта`;
  - `Наименование проекта`;
  - `Номер задачи`;
  - `Дата начала проекта`;
  - `Дата окончания проекта`;
  - `Место возникновения затрат (МВЗ)`;
  - `Наименование конструкторского бюро`;
  - `Управление`;
  - `Месяц`;
  - `Часы`.

Что изменено в `script.js`:

- добавлена функция `getSalesPlanWorksheetName()`;
- приоритет источника плана продаж теперь:
  - `ПП`;
  - затем запасные старые имена `План_продаж` и `План-продаж`;
- `getTechBlockSalesPlanByProject()` теперь читает лист через `getSalesPlanWorksheetName()`;
- для часов добавлен новый fallback-заголовок `Итого за 2025 год`, но основной источник остается `Часы`;
- расчет фактической загрузки центра в `getSpentProjectHoursByMonthForCenter()` теперь исключает все строки `Загрузка_ТехБлок` с признаком `Прогноз`, а не только будущие прогнозные строки;
- `getForecastProjectHoursByMonthForCenter()` больше не добавляет внеплановые прогнозные часы из `Загрузка_ТехБлок`;
- общая логика будущей нагрузки теперь опирается на `ПП[Часы]` по `ПП[Месяц]` и `ПП[Управление]`;
- текстовые подсказки и формулы в интерфейсе переведены с `План_продаж` на `ПП`;
- подсказка ячейки строки `прогноз / очередь` больше не показывает `прогноз из загрузки`.

Проверка:

- `node --check script.js` прошел успешно;
- структура текущего Excel-файла прочитана в режиме read-only через bundled Python/openpyxl, без изменения книги.

Вывод:

- расчет больше не смешивает часы из `ПП` с прогнозными часами из `Загрузка_ТехБлок`;
- лист плана продаж теперь ищется как `ПП`, при этом старые имена оставлены как совместимый fallback.

## 73. Старт шкалы `Общая загрузка по месяцам` на вкладке `ПП` с января текущего года

Дата фиксации: 29.06.2026 16:14 MSK

Пользователь попросила:

```text
на вкладке ПП в общей загрузке пусть тоже будет отсчет с начала текущего года
```

Что изменено в `script.js`:

- блок `Общая загрузка по месяцам` внутри `renderPlanFactHeatmapSummary()` больше не использует напрямую общий диапазон план-факта;
- добавлена функция `getProductionProgramMonthlyLoadRange(endDate)`;
- старт диапазона для шкалы теперь считается как `new Date(getReferenceDate().getFullYear(), 0, 1)`, то есть 1 января текущего года;
- конец диапазона остается общим `endDate` план-факта, если он позже старта, чтобы будущие месяцы плана не пропали.

Проверка:

- через Context7 сверено, что `Date` в JavaScript использует нулевую индексацию месяцев, поэтому январь задается как месяц `0`;
- `node --check script.js` прошел успешно.

Вывод:

- на вкладке `ПП` шкала общей загрузки теперь должна начинаться с января текущего года, а не с июля 2025;
- остальные блоки план-факта продолжают использовать свой общий диапазон 2025-2027.

## 74. Исправление группировки проектов по управлениям на вкладках центров

Дата фиксации: 29.06.2026 16:24 MSK

Пользователь указала:

```text
центры не сгруппированы, для ЦНГО их должно быть только 3, а сейчас очень много разделений и странных объединений
если проект есть в двух управлениях, то его нужно выводить и там и там, а не группировать по управлениям, где еще проект встречается
```

Что было найдено:

- в таблице проектов для вкладок центров данные собираются из `Проекты_ТехБлок`;
- у проекта с несколькими управлениями поле `management` собиралось как одна строка через запятую;
- `renderProjectDetails()` использовал эту строку как ключ группировки;
- из-за этого появлялись искусственные группы вида `Управление A, Управление B`, вместо отдельных групп `Управление A` и `Управление B`.

Что изменено в `script.js`:

- в `getProjectsFromTechBlockForCenter()` теперь дополнительно сохраняется массив `managements`;
- `management` оставлен как текстовое представление для совместимости и сортировки;
- `renderProjectDetails()` при группировке использует `getProjectDetailManagementGroups(project)`;
- если проект относится к двум управлениям, он добавляется в обе группы;
- объединенный заголовок из нескольких управлений больше не должен создаваться.

Проверка:

- через Context7 сверена стандартная работа `Map` и сортировки строк через `localeCompare`;
- `node --check script.js` прошел успешно.

Вывод:

- для `ЦНГО` таблица должна вернуться к реальным группам управлений;
- проекты с несколькими управлениями будут видны в каждом соответствующем управлении.

## 75. Пояснение расчета счетчиков проектов по статусу и типу

Дата фиксации: 29.06.2026 16:44 MSK

Пользователь спросила:

```text
как производится подсчет кол-ва проектов определенного статуса и типа?
```

Что проверено в `script.js`:

- счетчики в матрице блока `Проекты` строятся цепочкой:
  - `updateProjects()`;
  - `getProjectsFromDetails()`;
  - `getProjectsFromTechBlockForCenter()`;
- источник данных для этого блока — лист `Проекты_ТехБлок`;
- для текущей вкладки центра строки фильтруются по столбцу `Центр`;
- проект считается уникальным по ключу `центр + ID`;
- несколько строк одного проекта внутри центра сворачиваются в один проект;
- тип проекта берется из первого найденного непустого значения `Вид проекта`;
- статусы всех строк проекта агрегируются:
  - если у одного проекта есть и завершенные, и незавершенные строки, общий статус становится `В работе`;
  - иначе используется общий статус по строкам;
- в таблицу допускаются только статусы:
  - `В работе`;
  - `На паузе`;
  - `В очереди`;
  - `У заказчика`;
  - `Завершена`;
- счетчик в ячейке равен `projects.length` для группы `статус + тип`.

Важное уточнение:

- это количество уникальных проектов, а не количество строк в Excel;
- если проект есть в нескольких управлениях одного центра, в общей матрице статуса/типа он считается один раз;
- при раскрытии детализации такой проект выводится в каждом своем управлении.

Проверка:

- через Context7 сверены используемые базовые операции JavaScript: фильтрация массива, группировка объектов и подсчет длины массива.

## 76. Исправление счетчика `ЦНГО / Коммерческий проект / В работе`

Дата фиксации: 29.06.2026 16:55 MSK

Пользователь указала:

```text
у ЦНГО коммерческих проектов в работе =30 судя по листу Проекты_техблок, а на дашборде 32
```

Что было найдено:

- текущий дашборд считал `32`, потому что `getProjectsFromTechBlockForCenter()` сначала сворачивал строки по ключу `центр + ID`;
- после такого сворачивания разные управления, типы и статусы одного ID попадали в один проект;
- правило `getProjectTableAggregatedStatus()` дополнительно переводило проект в `В работе`, если среди строк были одновременно завершенные и незавершенные статусы;
- из-за этого в `В работе` ошибочно попадали проекты со смешанными статусами:
  - `5554`: `Завершена + Отменена`;
  - `11080`: `У заказчика + Завершена`;
  - `20357`: `У заказчика + Завершена`;
  - `21958`: `У заказчика + Завершена`;
  - `22942`: `На паузе + Завершена`;
- при этом ID `28749` имел строку `ЦНГО / Коммерческий проект / В работе`, но из-за сворачивания по ID мог попадать в другой тип проекта.

Что изменено в `script.js`:

- ключ группировки для таблицы проектов центра изменен с `центр + ID` на:
  - `центр`;
  - `ID`;
  - `управление`;
  - `вид проекта`;
  - нормализованный статус;
- добавлена функция `getProjectTableGroupKey()`;
- один и тот же ID теперь может быть отдельной строкой учета для разных управлений, типов или статусов;
- это соответствует логике листа `Проекты_ТехБлок` и предыдущему правилу: если проект есть в двух управлениях, он выводится и считается там и там.

Проверка:

- через Context7 сверены используемые операции группировки/фильтрации JavaScript;
- `node --check script.js` прошел успешно;
- контрольный пересчет по текущему `Dashboard_report_Technical_unit.xlsm` дал:
  - `ЦНГО / Коммерческий проект / В работе = 30`;
  - строк в листе по этому условию = `30`;
  - уникальных ID среди них = `28`.

Вывод:

- счетчик на дашборде должен стать `30`;
- теперь матрица статусов/типов считает строки учета по управлению, типу и статусу, а не один общий проект по ID.

## 77. Фильтр `Статус КД = Готово` в целевом показателе и индикатор просрочки завершения

Дата фиксации: 30.06.2026 12:37 MSK

Пользователь попросила:

```text
давай добавим еще один фильтр в расчеты целевого показателя: Статус КД - брать только значение Готово. если несколько задач для разных управлений и есть хоть один статус КД "готово" эта задача должна попасть в расчет.
Также учти в расчетах целевого показателя по часам, если разница часов =0, значит, что уложились в часы и это не минус

для завершенных задач в столбце Факт окончание добавь рядом с датой круглый индикатор, как в столбце остаток по часам, у тех задач, у кого факт окончания больше, чем Дата завершения работ по договору.
```

Что проверено:

- в текущем `Dashboard_report_Technical_unit.xlsm` на листе `Проекты_ТехБлок` столбец `Q` имеет заголовок `Статус КД`;
- в столбце `Q` есть значения `Готово`, `Нет`, `В работе`, `Нормоконтроль`, `Проверка`, `Командировка`, `Техконтроль`, `Утверждение`.

Что изменено в `script.js`:

- в `getTargetIndicatorProjectRecords()` добавлено поле `hasReadyKd`;
- при чтении строк проекта проверяется `Статус КД` / `Статус РКД` / `КД` / `Столбец 17`;
- если хотя бы в одной строке проекта значение равно `Готово`, у проекта ставится `hasReadyKd = true`;
- `isTargetIndicatorProjectStartedInYear()` теперь включает в расчет только проекты с `hasReadyKd === true`;
- для расчета попадания в норму часов добавлена функция `isTargetIndicatorProjectInHours()`;
- сравнение часов теперь явно идет через разницу `план - факт`, где `0` считается выполнением в норме;
- в таблице проектов для завершенных задач добавлена функция `getProjectTableFactEndDelayDays()`;
- в колонке `Факт окончание` рядом с датой появляется круглый красный индикатор, если `Факт окончание > Дата завершения работ по договору`;
- tooltip индикатора показывает, на сколько дней факт окончания позже договорной даты;
- подписи целевого показателя и фильтра проектов обновлены: теперь там указано условие `Статус КД «Готово»`.

Проверка:

- через Context7 сверена работа с массивами и датами в JavaScript;
- `node --check script.js` прошел успешно;
- Q1 текущего файла подтвержден как `Статус КД`.

## 78. Запуск локального сервера для проверки дашборда

Дата фиксации: 30.06.2026 12:40 MSK

Пользователь попросила:

```text
запусти сервер, чтобы проверить
```

Что сделано:

- проверено, что в проекте есть `local-server.mjs` и `index.php`;
- PHP в окружении не найден, поэтому использован проектный Node-сервер `local-server.mjs`;
- первая попытка запуска в sandbox дала `EPERM` на `127.0.0.1:8080`;
- сервер запущен с разрешением на локальный `listen`;
- адрес сервера: `http://127.0.0.1:8080/`;
- авторизация отключена;
- внешним браузером открыт URL `http://127.0.0.1:8080/`.

Проверка:

- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`;
- `curl -I http://127.0.0.1:8080/script.js` вернул `HTTP/1.1 200 OK`;
- `curl -I http://127.0.0.1:8080/file/Dashboard_report_Technical_unit.xlsm` вернул `HTTP/1.1 200 OK`.

## 79. Центрирование даты с индикатором просрочки в `Факт окончание`

Дата фиксации: 30.06.2026 12:42 MSK

Пользователь попросила:

```text
выровняй по центру дату с индикатором
```

Что изменено:

- в `script.js` контейнер даты с индикатором просрочки получил отдельный класс `project-fact-end-indicator-container`;
- в `style.css` для этого класса задано:
  - `justify-content: center`;
  - `width: 100%`;
- общий класс `project-hours-indicator-container` не изменен, поэтому колонка `Остаток по часам` сохраняет прежнее выравнивание вправо.

Проверка:

- через Context7 сверено центрирование flex-контейнера через `justify-content: center` и `align-items: center`;
- `node --check script.js` прошел успешно.

## 80. Единый стиль бейджей для даты, индикатора по дате и остатка часов

Дата фиксации: 30.06.2026 12:53 MSK

Пользователь попросила:

```text
давай вместо индикаторов сделаем более современный вариант отображения, может, в виде некоторых бейджев? если дата вышла за пределы, то сделать ее красной с фоном и жирным тексом, у часов аналогично
плюс еще для столбца Индикатор по дате тоже такой же стиль нужно применить
```

Что изменено:

- в `script.js` круглый индикатор в `Факт окончание` заменен на красный бейдж с самой датой, если `Факт окончание > Дата завершения работ по договору`;
- в `script.js` старый цветной элемент `Индикатор по дате` заменен на общий бейдж `project-value-badge`;
- в `script.js` колонка `Остаток по часам` больше не показывает текст + круг, а показывает само значение как бейдж;
- добавлена функция `getProjectTableBadgeTone(color)`, которая переводит текущие цвета индикаторов в модификаторы:
  - `danger`;
  - `warning`;
  - `success`;
  - `neutral`;
- в `style.css` добавлен общий стиль `project-value-badge` и модификаторы `project-value-badge--danger`, `--warning`, `--success`, `--neutral`;
- старые CSS-классы круглых индикаторов для этих колонок удалены из `style.css`, так как больше не используются в разметке.

Проверка:

- через Context7 сверено использование `inline-flex`, `border-radius`, фона и жирного текста для компактного бейджа;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8080/`.

## 81. Обновление подписи часов, легенды и риска в матрице проектов

Дата фиксации: 30.06.2026 13:18 MSK

Пользователь попросила:

```text
может, где индикатор часов, вместо минуса, какое-то другое обозначение сделать или добавить маленькую и светло-серую подпись "превышение", где желтые "приближается к лимиту", где зеленый ничего не писать

где легенда индикаторов тоже нужно адаптировать под новый вид

красные круглые точки на пересечении статуса и вида проекта тоже нужно осовременить
```

Что изменено:

- в `script.js` для колонки `Остаток по часам` красный бейдж теперь показывает абсолютное значение без минуса и подпись `превышение`;
- в `script.js` желтый бейдж остатка по часам показывает подпись `приближается к лимиту`;
- зеленый бейдж остатка по часам оставлен только со значением, без дополнительной подписи;
- tooltip для остатка по часам сохраняет исходное значение со знаком, чтобы расчет не терял смысл;
- в `script.js` легенда индикаторов переведена с круглых маркеров на бейджи, но текст подписей оставлен прежним:
  - `25-100% запас часов / резерв`;
  - `0-24% приближение к лимиту`;
  - `<0% перерасход`;
  - `нет данных нет плановых часов`;
- в матрице `Статус / Вид проекта` красная круглая точка заменена на мягкую красную заливку всей ячейки с проблемными проектами;
- для проблемной ячейки добавлен tooltip `Есть проблемные проекты: просрочка или перерасход`;
- в `style.css` добавлены стили для вертикального бейджа с подписью, бейджа легенды и критичной ячейки матрицы;
- неиспользуемый стиль круглого маркера легенды удален.

Проверка:

- через Context7 сверены flex-выравнивание, вложенная подпись в бейдже и styling table cells/background states;
- `node --check script.js` прошел успешно;
- локальный сервер запущен командой `node local-server.mjs`;
- сервер открылся на `http://127.0.0.1:8081/`;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 83. Переключатель группировки таблицы проектов по управлениям или задачам

Дата фиксации: 30.06.2026 13:31 MSK

Пользователь попросила:

```text
давай добавим кнопку, чтобы задачи в таблице пересобирались без группировки по управлениям и выводились общие значения: мин дата начала, макс даты окончания, если часы, то сумма
по умолчанию должна быть разбивка по управлоениям, как сейчас, но с возможностью перейти на сгруппированный вариант по задачам
```

Что изменено:

- в `script.js` добавлено состояние `projectsGroupedByTask`, по умолчанию `false`;
- в панель управления таблицей проектов добавлен сегментированный переключатель:
  - `По управлениям` — режим по умолчанию, сохраняет текущую разбивку;
  - `По задачам` — собирает строки без управления в ключе группировки;
- в режиме `По задачам` ключ агрегации строится по `центр + ID`, поэтому одна задача из нескольких управлений отображается одной строкой;
- при агрегации по задаче сохраняются текущие правила:
  - дата начала берется минимальная;
  - даты окончания берутся максимальные;
  - плановые и фактические часы суммируются;
  - статус агрегируется существующей логикой, где сочетание завершенных и незавершенных строк дает общий статус `В работе`;
- в режиме `По задачам` детализация не выводит заголовки управлений;
- в `style.css` добавлены стили сегментированного переключателя и мобильное поведение, чтобы кнопки не переполняли панель.

Проверка:

- через Context7 сверено использование кнопок с `aria-pressed` и flex-размещение сегментированного переключателя;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 84. Колонка `Управление` в режиме группировки проектов по задачам

Дата фиксации: 30.06.2026 13:37 MSK

Пользователь попросила:

```text
когда смотрим в режиме "по задачам" давай добавим столбец "управление" чтобы было понятно к каким управлениям принадлежат задачи и выведем этот столбец первым в списке
```

Что изменено:

- в `script.js` в детализации проектов добавлен флаг `showManagementColumn`, который включается только в режиме `По задачам`;
- в режиме `По задачам` в раскрытой таблице проектов первым столбцом выводится `Управление`;
- значение берется из уже агрегированного поля `project.management`, где перечислены управления задачи;
- для длинного списка управлений добавлен `title`, чтобы полный текст был доступен при наведении;
- в обычном режиме `По управлениям` таблица остается без дополнительной колонки, как раньше;
- в `style.css` добавлены отдельные правила ширин `projects-grid-header--with-management` и `projects-grid-row--with-management`, чтобы новая колонка не ломала существующие таблицы;
- отдельно учтен вариант строк без колонки `Индикатор по дате`.

Проверка:

- через Context7 сверено использование `flex-basis`, `min-width` и `max-width` для условных колонок flex-таблицы;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 85. Вертикальное выравнивание значений таблицы проектов по верхнему краю

Дата фиксации: 30.06.2026 13:43 MSK

Пользователь попросила:

```text
пусть значения в таблицах для всех столбцов будет выравнено по вертикали к верхнему краю
```

Что изменено:

- в `style.css` для ячеек раскрытой таблицы проектов добавлено `vertical-align: top`;
- flex-контейнеры внутри ячеек (`project-indicator-cell`, `project-planfact-container`) переведены с центрирования на `align-items: flex-start`;
- заголовки flex-таблицы также выравниваются по верхнему краю через `align-items: flex-start`;
- это влияет на оба режима таблицы проектов:
  - `По управлениям`;
  - `По задачам`.

Проверка:

- через Context7 сверено, что для flex-строк вертикальное выравнивание выполняется через `align-items: flex-start`, а для table-cell через `vertical-align: top`;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 86. Единые переключатели фильтров и полугодовые срезы целевого показателя

Дата фиксации: 30.06.2026 13:55 MSK

Пользователь попросила:

```text
сделай одной высоты два этих блока фильтров
давай переделаем по стилю блок "показать только проекты..." на такое же переключение, что и группировку по управлениям/задачам, чтобы было "Все задачи / Задачи УПЦ"

в расчеты еще нужно добавить фильтрацию, что первые целевые показатели были посчитаны для первого полугодия 2026 года, но во втором полугодии нужно будет сделать аналогично для задача второго полугодия 2026, поэтому в Блок "Целевой показатель" нужно добавить переключатель и, когда пройдет пол года, чтобы появился второй срез
```

Что изменено:

- в `script.js` checkbox `Показать только проекты из расчета целевого показателя` заменен на сегментированный переключатель `Все задачи / Задачи УПЦ`;
- переключатель `Все задачи / Задачи УПЦ` использует тот же общий стиль, что и `По управлениям / По задачам`, поэтому оба блока одной высоты;
- фильтр `Задачи УПЦ` теперь берет задачи из выбранного среза целевого показателя;
- добавлено состояние `targetIndicatorPeriodKey`, по умолчанию `2026-H1`;
- расчет целевого показателя переведен с полного 2026 года на выбранное полугодие:
  - `2026-H1`: 01.01.2026-30.06.2026;
  - `2026-H2`: 01.07.2026-31.12.2026;
- второй срез `II полугодие` появляется в блоке `Целевой показатель` только после наступления 01.07.2026;
- в блок `Целевой показатель` добавлен переключатель полугодий;
- при переключении полугодия обновляются и сам целевой показатель, и таблица проектов, если включен фильтр `Задачи УПЦ`;
- старые CSS-правила для checkbox-фильтра удалены.

Проверка:

- через Context7 сверены flex-переключатели, `aria-pressed` и единая высота кнопочных сегментов;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 87. Год в подписи переключателя полугодия целевого показателя

Дата фиксации: 30.06.2026 13:57 MSK

Пользователь попросила:

```text
рядом с "I полугодие" добавь год
```

Что изменено:

- в `script.js` подпись кнопки первого среза изменена на `I полугодие 2026`;
- подпись второго среза также сделана полной: `II полугодие 2026`;
- логика расчета и доступности срезов не изменялась.

Проверка:

- через Context7 сверено, что кнопки должны иметь самодостаточный текст;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`.

## 88. Фильтр просрочек по дате и часам для таблицы проектов

Дата фиксации: 30.06.2026 14:14 MSK

Пользователь попросила:

```text
добавь фильтр, чтобы можно было в задачах УПЦ увидеть только задачи с просрочкой по дате/часам/дате и часам
давай добавим фильтр не только для задач УПЦ, а для всех
```

Что изменено:

- в `script.js` добавлено состояние `projectsOverdueFilter`, по умолчанию `all`;
- в панель фильтров таблицы проектов добавлен сегментированный фильтр просрочек:
  - `Все сроки`;
  - `Просрочка даты`;
  - `Просрочка часов`;
  - `Дата и часы`;
- фильтр применяется ко всем задачам после остальных ограничений, поэтому работает:
  - в режиме `Все задачи`;
  - в режиме `Задачи УПЦ`;
  - при группировке `По управлениям`;
  - при группировке `По задачам`;
- просрочка по дате определяется по уже существующей логике:
  - для завершенных задач через `Факт окончание > Дата завершения работ по договору`;
  - для остальных через красный `dateColor`;
- просрочка по часам определяется через красный `hoursColor`, то есть отрицательный остаток часов;
- фильтр `Дата и часы` оставляет только задачи, где одновременно есть просрочка по дате и перерасход часов.

Проверка:

- через Context7 сверено использование сегментированных toggle-кнопок с `aria-pressed`;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 89. Сохранение раскрытой таблицы проектов при применении фильтров

Дата фиксации: 30.06.2026 14:18 MSK

Пользователь попросила:

```text
можешь сделать, чтобы таблица менялась, при применении фильтров, не сворачиваясь каждый раз?
```

Что изменено:

- в `script.js` добавлено состояние `expandedProjectDetailsKey`, которое хранит открытую пару `Статус / Вид проекта`;
- при клике по ячейке матрицы проектов ключ раскрытия сохраняется;
- после `updateProjects()` вызывается `restoreExpandedProjectDetails()`;
- если после применения фильтра та же пара `Статус / Вид проекта` осталась в таблице, раскрытый блок автоматически открывается снова;
- если фильтр полностью убрал эту пару из выборки, сохраненный ключ сбрасывается;
- синхронизирована старая глобальная функция `window.toggleProjectDetails()`, чтобы она тоже обновляла тот же ключ раскрытия.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 90. Адаптивные правки для фильтров, таблицы проектов и шкалы загрузки

Дата фиксации: 30.06.2026 14:28 MSK

Пользователь попросила:

```text
проверь и поправь адаптивные стили для разных экранов
```

Что изменено:

- в `style.css` для панели фильтров проектов добавлены ограничения `max-width: 100%` и `min-width: 0`, чтобы новые сегментированные переключатели не распирали контейнер;
- на ширине до `1199px` фильтр просрочек переносится на отдельную строку, а панель фильтров выравнивается влево;
- на мобильных ширинах внешняя матрица проектов больше не получает принудительный `min-width`, поэтому не создает лишний горизонтальный скролл всего блока;
- детальная таблица проектов сохранила горизонтальный скролл через `.projects-details-wrapper`, потому что у нее много колонок и это безопаснее для читаемости;
- на очень маленьких экранах сегментированные кнопки занимают всю ширину, равномерно делятся по строке и могут переносить длинный текст;
- для блока `Общая загрузка по месяцам` на мобильных добавлен горизонтальный скролл шкалы месяцев, чтобы месяцы оставались в одну строку и не ломали ширину страницы.

Проверка:

- через Context7 сверены responsive media queries, flex-wrap и overflow-подход для широких таблиц;
- предпринята попытка автоматического Playwright-аудита на разных viewport; встроенный Playwright-браузер не установлен, а системный Chrome не запустился в headless-режиме из окружения, поэтому проверка продолжена статическим CSS-аудитом и локальным браузером;
- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 91. Фильтр просрочек `Дата или часы`

Дата фиксации: 30.06.2026 14:38 MSK

Пользователь попросила:

```text
фильтр с просрочкой дат и по часам параметр "Дата и часы" сделай не через "и", а через "или", чтобы показались все записи и с просрочкой по дате и по часам, но с параметром через или
```

Что изменено:

- в `script.js` подпись фильтра изменена с `Дата и часы` на `Дата или часы`;
- логика фильтра `date-hours` изменена с условия `hasDateOverdue && hasHoursOverdue` на `hasDateOverdue || hasHoursOverdue`;
- теперь этот параметр показывает все задачи, где есть просрочка по дате или перерасход по часам.

Проверка:

- через Context7 сверено, что текст кнопки должен быть самодостаточным и понятным;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 92. Исключение `Статус КД` из целевого показателя для ЛОИ и УВП

Дата фиксации: 30.06.2026 15:03 MSK

Пользователь попросила:

```text
измени расчет для ЛОИ и УВП, у них не бывает статуса КД, поэтому там смотрим только по статусу проекта, которые завершены
```

Что изменено:

- в `script.js` добавлен список исключений `TARGET_INDICATOR_NO_KD_CENTERS = new Set(['ЛОИ', 'УВП'])`;
- добавлена функция `isTargetIndicatorProjectEligibleByKdOrStatus(project)`;
- для обычных центров целевой показатель по-прежнему требует `Статус КД = Готово`;
- для `ЛОИ` и `УВП` условие `Статус КД = Готово` не применяется;
- для `ЛОИ` и `УВП` задача допускается в расчет только если агрегированный статус проекта завершенный;
- поясняющий текст в блоке `Целевой показатель` обновлен: теперь указано, что для `ЛОИ` и `УВП` вместо статуса КД используется статус проекта `Завершена`.

Проверка:

- через Context7 сверено использование `Set.has()` для явного списка исключений;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 82. Сокращение подписей легенды и перенос фильтра в строку легенды

Дата фиксации: 30.06.2026 13:24 MSK

Пользователь попросила:

```text
тк процент уже указан в цветном бейдже, рядом из описания проценты можно убрать и оставить т только:
"запас часов / резерв
приближение к лимиту
перерасход
нет данных нет плановых часов"

легенду индикаторов и фильтр "показать только проекты..." размести на одной строке
```

Что изменено:

- в `script.js` текст рядом с бейджами легенды больше не дублирует процент или статус из самого бейджа;
- итоговая легенда читается как:
  - бейдж `25-100%` + `запас часов / резерв`;
  - бейдж `0-24%` + `приближение к лимиту`;
  - бейдж `<0%` + `перерасход`;
  - бейдж `нет данных` + `нет плановых часов`;
- в `index.php` добавлен слот `projects-target-filter-slot` внутри блока легенды;
- в `script.js` фильтр `Показать только проекты из расчета целевого показателя` теперь рендерится в этот слот, а не отдельной строкой над таблицей;
- в `style.css` блок легенды превращен в горизонтальную панель: слева заголовок и бейджи, справа фильтр;
- для очень маленьких экранов добавлен безопасный перенос: легенда и фильтр занимают всю ширину и не переполняют блок.

Проверка:

- через Context7 сверено использование `flex`, `gap`, `flex-wrap` и `justify-content: space-between` для горизонтальной панели с переносом;
- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8081/` вернул `HTTP/1.1 200 OK`;
- страница открыта во внешнем браузере по адресу `http://127.0.0.1:8081/`.

## 93. Строгий фильтр видов проектов для расчета УПЦ

Дата фиксации: 01.07.2026 08:48 MSK

Пользователь попросила:

```text
в расчеты упц для ЛОИ попали и другие виды проектов, помимо коммерческих и инвестиционных
```

Что изменено:

- через Context7 сверена логика `String.prototype.startsWith()` / строгой проверки строк вместо широкого поиска подстроки;
- в `script.js` добавлен явный набор допустимых видов проектов для целевого показателя:
  - `коммерческий`;
  - `коммерческий проект`;
  - `инвестиционный`;
  - `инвестиционный проект`;
- функция `isTargetIndicatorProjectTypeIncluded()` больше не использует `includes('коммерческ')` и `includes('инвестицион')`;
- теперь в расчет УПЦ и фильтр `Задачи УПЦ` не должны попадать сторонние виды проектов, например значения, где `коммерческ` встречается только как часть другого слова.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен командой `node local-server.mjs`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 94. Построчный фильтр видов проектов для `Задачи УПЦ`

Дата фиксации: 01.07.2026 08:55 MSK

Пользователь показала, что на вкладке `ЛОИ` через фильтр `Задачи УПЦ` в таблицу попала `Предпроектная подготовка`, хотя в расчете должны быть только коммерческие и инвестиционные проекты.

Что изменено:

- через Context7 сверена логика `Set.has()` и `Array.prototype.filter()` для строгих булевых предикатов;
- в `getTargetIndicatorProjectRecords()` строки листа `Проекты_ТехБлок` с недопустимым `Вид проекта` теперь исключаются до агрегации задачи;
- из расчета целевого показателя больше не суммируются даты, статусы и часы строк `Предпроектная подготовка`, `РИД`, `Другие проекты` и прочих типов;
- в `getProjectsFromTechBlockForCenter()` при включенном фильтре `Задачи УПЦ` добавлен построчный отсев по `Вид проекта`;
- теперь даже если один и тот же ID задачи встречается в разных видах проектов, в таблице `Задачи УПЦ` остаются только строки коммерческого и инвестиционного типа.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 95. Проверка запущенного локального сервера

Дата фиксации: 01.07.2026 09:00 MSK

Пользователь попросила:

```text
запусти сервер
```

Что сделано:

- через Context7 сверена базовая логика Node.js `server.listen()` для локального HTTP-сервера;
- проверено, что локальный сервер уже запущен на `http://127.0.0.1:8080/`;
- дополнительный запуск не потребовался.

Проверка:

- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 96. Уникальный счетчик задач в матрице проектов

Дата фиксации: 01.07.2026 09:10 MSK

Пользователь попросила:

```text
пусть в фильтре, где мы меняем группировку по управлениям или по задачам, общее кол-во задач пусть будет уникальным, иначе, когда группировка по управлениям включается, задач становится якобы больше, хотя просто одна и та же задача у нескольких управлений
```

Что изменено:

- через Context7 сверено использование `Set.size` для подсчета уникальных значений;
- в `script.js` добавлена функция `getUniqueProjectTaskCount(projects)`;
- в объект проекта для таблицы добавлен `targetKey`, чтобы одна задача считалась один раз независимо от количества управлений;
- счетчик в ячейках матрицы `Статус / Вид проекта` теперь считается по уникальным задачам, а не по количеству строк/групп;
- счетчик, который используется для фильтра `Задачи УПЦ`, также переведен на уникальные задачи;
- раскрывающиеся строки таблицы остаются в выбранном режиме группировки, меняется только отображаемое количество задач.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 97. Прогноз/очередь в общей загрузке за все месяцы шкалы

Дата фиксации: 01.07.2026 09:27 MSK

Пользователь попросила:

```text
пусть в блоке "Общая загрузка по месяцам" в шкале прогноз / очередь данные показывает за все периоды, по которым есть дата, но чтобы он был ограничен шкалой факт, т.е. с января 2026 года в данном случае
```

Что изменено:

- через Context7 сверена базовая работа JavaScript `Date` для месячных диапазонов;
- в `renderCenterMonthlyLoadSection()` при расчете `loadMonths` добавлен флаг `showAllForecastHours: true`;
- строка `прогноз / очередь` в блоке `Общая загрузка по месяцам` теперь показывает плановые часы за все месяцы, которые попали в шкалу по данным;
- диапазон шкалы остается ограничен текущим годом факта, то есть в текущем сценарии начинается с января 2026 года;
- логика вкладки `ПП` не менялась, потому что там этот режим уже был включен.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 98. Новые правила расчета УПЦ и переименование плановой даты

Дата фиксации: 03.07.2026 09:04 MSK

Пользователь попросила:

```text
расчеты упц:
- для УВП вообще нужно этот показатель убрать и фильтр связанный с упц тоже
- на странице ТехБлока упц считать нужно не для каждого центра отдельно, а отдельно для ЛОИ и задачи без разделения на оставшиеся центры (т.е. без лои и увп). сейчас в цнго завершились 3, а в цдцм 4, значит всего 7 задач, и 4 в сумме не вышли за сроки и 7 по часам

переименуй везде столбец "Дата завершения работ по договору" на "План дата завершения этапа по графику проекта"
```

Что изменено:

- через Context7 сверена логика агрегации массивов и накопления итогов по группам;
- в `script.js` добавлен список исключенных из УПЦ центров `TARGET_INDICATOR_EXCLUDED_CENTERS = new Set(['УВП'])`;
- для `УВП` блок `Целевой показатель` больше не отображается;
- на вкладке `УВП` скрывается фильтр `Все задачи / Задачи УПЦ`, а если он был включен ранее, состояние сбрасывается;
- правило отсутствия `Статус КД` оставлено только для `ЛОИ`;
- на вкладке `ТехБлок` целевой показатель теперь выводит агрегат `Остальные центры (без ЛОИ и УВП)` и отдельную строку `ЛОИ`;
- показатели `Нач.`, `Зав.`, `Срок`, `Часы` для остальных центров суммируются без разбивки на ЦНГО/ЦДЦМ/ЦДПМ/ЦОТ;
- список `ID задач в расчете` на `ТехБлок` группируется по тем же двум группам;
- видимые заголовки таблицы проектов переименованы с `Дата завершения работ по договору` на `План дата завершения этапа по графику проекта`;
- новый заголовок добавлен как основной алиас чтения Excel, старый заголовок оставлен fallback-ом для совместимости со старыми файлами;
- tooltip просроченного `Факт окончание` теперь говорит о превышении плановой даты завершения этапа.

Проверка:

- `node --check script.js` прошел успешно;
- локальный сервер запущен командой `node local-server.mjs`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 99. Названия групп УПЦ на вкладке ТехБлок

Дата фиксации: 03.07.2026 09:11 MSK

Пользователь попросила:

```text
в техблоке названия центров укажи "Центры КБ" и "ЛОИ"
```

Что изменено:

- через Context7 сверена базовая логика строковых `const`-значений;
- отображаемое название агрегированной группы УПЦ на вкладке `ТехБлок` изменено на `Центры КБ`;
- строка `ЛОИ` остается отдельной строкой;
- поясняющий текст целевого показателя обновлен: `Центры КБ считаются суммарно без ЛОИ и УВП, ЛОИ считается отдельно`;
- внутренний ключ сортировки `Остальные центры` не менялся, потому что он не показывается пользователю.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 100. Короткое название ЛОИ в целевом показателе ТехБлока

Дата фиксации: 03.07.2026 09:16 MSK

Пользователь показала, что в целевом показателе на вкладке `ТехБлок` строка `ЛОИ` все еще отображается полным названием `Управление лабораторными исследованиями`.

Что изменено:

- через Context7 сверена логика возврата object literal со строковым label;
- в `getTargetIndicatorDisplayGroup()` для режима `ТехБлок` label центра `ЛОИ` изменен с `getProjectStageFullLabel('ЛОИ')` на короткое значение `ЛОИ`;
- теперь в таблице целевого показателя на `ТехБлок` должны отображаться строки `Центры КБ` и `ЛОИ`.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 101. Карточки УПЦ на вкладке ТехБлок вместо таблицы

Дата фиксации: 03.07.2026 09:36 MSK

Пользователь попросила:

```text
теперь на вкладке техблок не понятно сколько проектов из скольки в срок или по часам уложились. давай сделай блоками как на срине, без начато и завершено, но в два ряда: 1 рад - центры кб, 2 ряд - лои
```

Что изменено:

- через Context7 сверена логика `Array.prototype.map()` для сборки HTML по строкам данных;
- в `renderTargetIndicatorContent()` для режима `ТехБлок` таблица `Центр / Нач. / Зав. / Срок / Часы` заменена на карточный вид;
- каждая группа УПЦ выводится отдельной строкой:
  - `Центры КБ`;
  - `ЛОИ`;
- в каждой строке теперь две карточки:
  - `В срок`;
  - `В норме часов`;
- карточки показывают процент и подпись `X из Y`, где `Y` — количество завершенных задач группы;
- блоки `Начато` и `Завершено` для режима `ТехБлок` больше не выводятся;
- в `style.css` добавлены стили `target-indicator-groups`, `target-indicator-group-row`, `target-indicator-group-row__title`, `target-indicator-metrics--grouped`.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 102. Адаптация карточек УПЦ на узких экранах

Дата фиксации: 03.07.2026 09:42 MSK

Пользователь показала, что на узкой ширине названия групп `Центры КБ` и `ЛОИ` занимают отдельные большие блоки, а карточки процентов выглядят сжато.

Что изменено:

- через Context7 сверена адаптивная схема CSS Grid с `minmax()` и точечными media-query;
- названия групп УПЦ оставлены над карточками обычной текстовой строкой, без рамки, фона и карточного контейнера;
- строки `Центры КБ` и `ЛОИ` теперь выглядят как заголовки к двум метрикам, а не как третья карточка;
- для двух карточек `В срок` и `В норме часов` задана устойчивая сетка `repeat(2, minmax(...))`;
- на экранах до `479px` уменьшены отступы и размер текста внутри карточек, чтобы значения вроде `100%` не выходили за пределы блока.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 103. Проекты на вкладке ТехБлок и скрытие общей загрузки на центрах

Дата фиксации: 08.07.2026 12:00 MSK

Пользователь попросила:

```text
убери из вкладок центров блок Общая загрузка по месяцам.

На вкладку ТехБлок выведи таблицу с проектами, только фильтр, где делим задачи на все или по управлениям, нужно изменить на "По центрам / По задачам": если в одном центре задача у нескольких управлений - объединяем значения дат и часов. потом задача может быть и у разных центров, их тоже нужно будет объединить по датам и часам
```

Что изменено:

- через Context7 сверена стандартная агрегация JavaScript через `Map`, `Set` и суммирование значений;
- `renderCenterMonthlyLoadSection()` больше не выводит блок `Общая загрузка по месяцам` на вкладках центров;
- `updateProjects()` больше не скрывает блок `Проекты` на вкладке `ТехБлок`;
- для `ТехБлок` переключатель группировки проектов переименован в `По центрам / По задачам`;
- на вкладках центров подписи переключателя остаются прежними: `По управлениям / По задачам`;
- для `ТехБлок` источник таблицы проектов теперь читает все центры из листа `Проекты_ТехБлок`;
- в режиме `По центрам` ключ агрегации строится как `центр + ID`, поэтому строки одной задачи внутри центра объединяются по управлениям;
- в режиме `По задачам` ключ агрегации строится только по `ID`, поэтому одна задача из разных центров объединяется по датам и часам;
- при объединении сохраняются правила: минимальная дата начала, максимальные даты окончаний, сумма плановых и фактических часов, агрегированный статус;
- в раскрытой таблице проектов на `ТехБлок` добавлена первая колонка `Центр`, где для режима `По задачам` может отображаться несколько центров.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- локальный сервер запущен командой `node local-server.mjs`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 104. Группировка таблицы проектов ТехБлока по центрам

Дата фиксации: 08.07.2026 12:18 MSK

Пользователь уточнила:

```text
измени отображение в таблице: если выводим по центрам, то нужно группировать задачи по центрам и полное название центра выводить над списком задач (как это сделано для управлений центра) и столбец ЦЕНТР не выводить. А вот если группируем без разбивки на центры и объединяем задачи, то нужен столбец ЦЕНТР, с краткими названиями центров
```

Что изменено:

- через Context7 сверена работа `Map` для группировки записей по ключу;
- в `renderProjectDetails()` для вкладки `ТехБлок` добавлен отдельный режим группировки деталей по центрам;
- при переключателе `По центрам` задачи в раскрытой таблице группируются секциями по центрам;
- над каждой секцией выводится полное название центра через `getProjectStageFullLabel()`;
- в режиме `По центрам` колонка `Центр` больше не выводится, потому что центр уже указан заголовком группы;
- при переключателе `По задачам` секции по центрам не создаются, задачи остаются объединенными по ID;
- в режиме `По задачам` первая колонка `Центр` остается и показывает краткие коды центров, входящих в объединенную задачу;
- добавлен helper `getProjectDetailCenterGroups()` для устойчивого получения центров из агрегированной строки проекта.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 105. Стабильный счетчик уникальных задач в таблице проектов

Дата фиксации: 08.07.2026 12:27 MSK

Пользователь попросила:

```text
в таблицах проектов счетчик кол-ва задач не должен меняться в зависимости от переключения отображения задач, надо считать уникальных задач сколько, а не кол-во записей
```

Что изменено:

- через Context7 сверена работа `Set.size` для подсчета уникальных значений;
- в `getUniqueProjectTaskCount()` изменен приоритет ключа уникальности;
- теперь счетчик сначала использует `taskKey` или нормализованный `ID` задачи;
- `targetKey`, который включает центр, оставлен только fallback-ом для старых/неполных объектов;
- за счет этого на вкладке `ТехБлок` счетчик в матрице не должен меняться между режимами `По центрам` и `По задачам`, даже если одна задача встречается в нескольких центрах.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 106. Счетчик матрицы проектов отделен от режима отображения

Дата фиксации: 08.07.2026 12:37 MSK

Пользователь показала на примере `ЦНГО`, что счетчик все еще меняется между режимами `По управлениям` и `По задачам`: например, `Коммерческий проект` менялся с `28` на `32`.

Причина:

- при режиме `По задачам` строки сначала агрегировались по задаче;
- после агрегации у задачи мог измениться общий статус или тип ячейки;
- из-за этого счетчик считал не только уникальные ID, но и уже измененную принадлежность задачи к ячейке матрицы.

Что изменено:

- через Context7 сверена работа `Set.size` и группировка значений через `Map`/object;
- `getProjectsFromDetails()` теперь собирает два набора данных:
  - отображаемый набор — зависит от переключателя `По управлениям/По центрам / По задачам`;
  - счетный набор — всегда строится в стабильном детальном режиме без агрегации `По задачам`;
- добавлена функция `getProjectTableStableCountsByKey()`;
- числа в ячейках матрицы теперь берутся из `countsByKey`, а не из текущего отображаемого массива;
- `targetProjectVisibleCount` для фильтра `Задачи УПЦ` тоже берется из стабильного счетного набора;
- оси матрицы `Статус / Вид проекта` строятся по объединению отображаемого и счетного набора, чтобы ячейки не пропадали при переключении режима.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 107. Доступность раскрытия ячеек матрицы проектов при стабильном счетчике

Дата фиксации: 08.07.2026 12:45 MSK

Пользователь показала, что после стабилизации счетчика часть ячеек матрицы остается без раскрытия при переключении `По центрам / По задачам`; пример — `Предпроектная подготовка` в статусе `В очереди`.

Причина:

- число в ячейке уже бралось из стабильного счетного набора;
- но кликабельность, стрелка и HTML деталей все еще зависели только от текущего отображаемого набора;
- если в текущем режиме отображения проект агрегировался в другую ячейку, старая ячейка показывала число, но не имела данных для раскрытия.

Что изменено:

- `getProjectsFromDetails()` теперь возвращает не только `countsByKey`, но и `stableProjectsByKey`;
- добавлена функция `getProjectTableProjectsByKey()` для стабильной группировки проектов по `Статус||Вид проекта`;
- при отрисовке матрицы ячейка считается доступной, если в ней есть проекты либо в отображаемом наборе, либо в стабильном наборе;
- для раскрытой детализации используется отображаемый набор, а если он пустой — fallback на стабильный набор;
- критичная подсветка ячеек также проверяет fallback-детали, чтобы проблемные проекты не терялись при переключении режима.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 108. Квартальная динамика на месте месячной загрузки центров

Дата фиксации: 08.07.2026 12:55 MSK

Пользователь попросила:

```text
на место блока, где раньше был блок Общая загрузка по месяцам на вкладках центров, пусть будет блок "Динамика загрузки по кварталам". только надо изменить, чтобы отображал только 4 квартала, но при этом, если за последнйи выведенный квартал загрузка падает, потому что там указаны будущие периоды, то она не должны падать вниз, а оставаться тогда на уровне с прошлой
```

Что изменено:

- через Context7 сверена работа `Array.slice()` для ограничения списка записей и `Date` для определения квартального диапазона;
- в `renderCenterMonthlyLoadSection()` на вкладках центров вместо скрытого блока `Общая загрузка по месяцам` теперь выводится `Динамика загрузки по кварталам`;
- для центров используется отдельный canvas `center-quarterly-trend-chart`, чтобы не конфликтовать с графиком `ТехБлока`;
- добавлена функция `getCenterQuarterlyTrendDataFromTables(centerKey)`;
- квартальная динамика центра считается из плоских таблиц:
  - фактические часы — из `Загрузка_ТехБлок` по центру;
  - фонд часов — из `Сотрудники_ТехБлок` и `Табель` через `getTechBlockCenterCapacityByMonth()`;
- добавлен лимит `QUARTERLY_TREND_DISPLAY_LIMIT = 4`;
- добавлена функция `getQuarterlyTrendDisplayQuarters()`, которая оставляет только последние 4 квартала;
- если последний отображаемый квартал относится к текущему/будущему периоду и его процент ниже предыдущего квартала, визуальная точка удерживается на уровне предыдущего квартала;
- такое же ограничение до 4 кварталов и правило удержания применены к квартальной динамике на вкладке `ТехБлок`.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 109. Удаление квартальной динамики с вкладки ТехБлок

Дата фиксации: 08.07.2026 13:05 MSK

Пользователь показала пустой блок `Квартальная динамика пока недоступна` на вкладке `ТехБлок` и попросила убрать его.

Что изменено:

- через Context7 сверена базовая логика скрытия блока через `display: none` и очистку содержимого;
- `renderQuarterlyTrendSection()` больше не рендерит блок квартальной динамики в контейнер `#quarterly-trend-section`;
- контейнер очищается, скрывается и уничтожает chart id `quarterly-trend-chart`;
- квартальная динамика на вкладках центров остается без изменений, потому что она рендерится отдельно через `#center-monthly-load-section` и canvas `center-quarterly-trend-chart`.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 110. Возврат квартальной динамики на ТехБлок при наличии данных

Дата фиксации: 08.07.2026 13:12 MSK

Пользователь уточнила, что на `ТехБлоке` квартальная динамика раньше отображалась нормально, а после последней правки перестала отображаться совсем.

Причина:

- предыдущая правка слишком широко скрыла весь контейнер `#quarterly-trend-section`;
- нужно было убрать только пустую заглушку `Квартальная динамика пока недоступна`, а не сам график при наличии данных.

Что изменено:

- через Context7 сверена логика `display: none` для условного скрытия блока и `requestAnimationFrame()` перед созданием canvas-графика;
- `renderQuarterlyTrendSection()` снова рендерит квартальную динамику на вкладке `ТехБлок`, если есть данные;
- если квартальных данных нет, контейнер очищается и скрывается без пустого блока-заглушки;
- ограничение до 4 кварталов и правило удержания последней точки от резкого падения сохранены.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 111. Удаление верхней пустой заглушки квартальной динамики

Дата фиксации: 08.07.2026 13:20 MSK

Пользователь уточнила, что должна остаться только нижняя часть с диаграммой, а верхнюю пустую заглушку `Квартальная динамика пока недоступна` нужно удалить.

Что изменено:

- через Context7 сверена логика очистки DOM-контейнера и скрытия элемента через `display: none`;
- в `renderCenterMonthlyLoadSection()` добавлена проверка `quarters.length === 0`;
- если для верхнего контейнера `#center-monthly-load-section` нет квартальных данных, контейнер очищается и скрывается;
- общий `renderQuarterlyTrendMarkup()` больше не вызывается для пустого верхнего контейнера, поэтому заглушка не рисуется;
- нижний график квартальной динамики остается без изменений.

Проверка:

- `node --check script.js` прошел успешно;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 112. Проверка расхождения счетчика коммерческих проектов в работе

Дата фиксации: 08.07.2026 13:35 MSK

Пользователь спросила, почему сумма задач всех центров по ячейке `Коммерческий проект / В работе` дает `160`, а на вкладке `ТехБлок` отображается `168`.

Что проверено:

- через Context7 сверена работа `Set.size` и `Map` для уникальных подсчетов;
- прочитан файл `Dashboard_report_Technical_unit.xlsm`, лист `Проекты_ТехБлок`;
- воспроизведены две текущие ветки подсчета:
  - вкладки центров: группировка по `центр + ID + управление + вид проекта + статус`;
  - вкладка `ТехБлок`: стабильный счетчик по агрегированному ключу `центр + ID`;
- рассчитаны значения по текущей логике:
  - `ЦНГО = 28`;
  - `ЦДЦМ = 47`;
  - `ЦДПМ = 16`;
  - `ЦОТ = 14`;
  - `ЛОИ = 36`;
  - `УВП = 19`;
  - сумма центров = `160`;
  - вкладка `ТехБлок` по текущей логике = `168` уникальных ID.

Вывод:

- расхождение возникает не из-за простой ошибки сложения, а из-за разных баз классификации;
- на `ТехБлоке` задача сначала агрегируется по `центр + ID`, а затем ей назначается общий статус и первый найденный вид проекта;
- если у одного ID есть строки со статусом `Завершена` и пустым/незавершенным статусом, функция `getProjectTableAggregatedStatus()` относит общий статус к `В работе`;
- из-за этого часть задач, которые на вкладках центров не попадают в ячейку `Коммерческий проект / В работе`, после агрегации на `ТехБлоке` попадают туда;
- также есть обратные случаи, когда строка `Коммерческий проект / В работе` на центре есть, но на `ТехБлоке` после агрегации ID получает другой первый вид проекта.

Примеры выявленных ID:

- попадают на `ТехБлок` в `Коммерческий проект / В работе`, но не входят в объединение этой ячейки по центрам: `5554`, `9137`, `11080`, `17281`, `20357`, `20631`, `21958`, `25966`;
- есть в ячейке центра, но не попадают в эту ячейку на `ТехБлоке` после агрегации: `23588`, `28749`, `28866`, `29923`, `30331`.

Рекомендация для следующей правки:

- если `ТехБлок` должен быть именно суммой центров, счетчик `ТехБлока` нужно считать по той же детальной классификации, что и центры, а не по предварительно агрегированному `центр + ID`;
- тогда ячейка `Коммерческий проект / В работе` на `ТехБлоке` будет совпадать с суммой центров `160`.

## 113. Единый набор задач для матрицы проектов и диагностика спорных строк

Дата фиксации: 08.07.2026 13:55 MSK

Пользователь уточнила:

```text
количество задач и сами задачи должны совпадать и должен быть один и тот же набор, но с разными группировками и фильтрами под вкладки. никакие задачи не должны пропадать. если есть задачи, с которыми непонятно что делать, вынеси их в блок ниже, который будет по умолчанию скрыт, и выпиши задачи с их проблемами, из-за чего не удалось их использовать в общей таблице проектов
```

Что изменено:

- через Context7 сверена работа `Map`, `Set` и HTML `details/summary` для скрытого диагностического блока;
- матрица проектов теперь строится из одного канонического детального набора;
- для `ТехБлока` детальный ключ в режиме основы изменен на `центр + ID + управление + вид проекта + статус`, как на вкладках центров;
- переключатель `По центрам / По задачам` больше не меняет принадлежность задачи к ячейке матрицы;
- режим `По задачам` теперь применяется внутри раскрытой ячейки через `aggregateProjectDetailRowsByTask()`;
- счетчик матрицы теперь использует `countKey = центр + ID`, чтобы:
  - не раздувать задачу из-за нескольких управлений внутри центра;
  - не схлопывать одну задачу, если она есть в нескольких центрах;
  - получать сумму, совпадающую с вкладками центров;
- удалена зависимость счетчика и раскрытия от отдельного стабильного fallback-набора, который мог показывать число без полного совпадения раскрытой детализации;
- добавлен скрытый по умолчанию блок `Задачи для проверки`;
- в блок диагностики попадают задачи/строки с проблемами:
  - несколько видов проекта у одного `центр + ID`;
  - несколько статусов у одного `центр + ID`;
  - строки со статусом вне общей матрицы;
  - строки без центра или ID;
- для диагностического блока добавлены стили `project-diagnostics`.

Ожидаемый эффект:

- ячейка `Коммерческий проект / В работе` на `ТехБлоке` должна считаться из того же набора, что и сумма центров, то есть для текущего файла должна совпасть с суммой `160`;
- задачи, которые нельзя однозначно использовать в общей матрице, не теряются молча, а показываются в свернутом блоке ниже.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 114. Исключение отмененных строк из таблицы проектов и задач для проверки

Дата фиксации: 08.07.2026 11:25 MSK

Пользователь уточнила:

```text
в задачи для проверки не нужно выводить задачи, если у нее статус Отменена. Если в каком-то центре задача отменена, а у другого центра в работе, значит, она в работе и выводим все центры, где нет статуса Отменена (отмененные задачи как бы полностью игнорируем)
```

Что изменено:

- через Context7 сверена семантика `Array.prototype.filter()` и предикатов исключения строк из выборки;
- добавлен helper `shouldIgnoreProjectTableRowByStatus()`;
- строки со статусом `Отменена` / `Отменен` / `Отменено` теперь исключаются до группировки проектов;
- отмененные строки не попадают в матрицу проектов, раскрытые таблицы и блок `Задачи для проверки`;
- если один центр по задаче отменен, а другой центр по той же задаче находится в работе или другом разрешенном статусе, в таблице остается только неотмененная часть;
- диагностика конфликтов статусов и видов проекта теперь строится без учета отмененных строк, чтобы отмена не создавала ложную проблему.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 115. Уточнение списка задач для проверки

Дата фиксации: 08.07.2026 11:33 MSK

Пользователь уточнила:

```text
Не выводи в списке проблемных задач задачи, у которых отличаются статусы, тк статусы мы берем по принципу приоритетности. Если ты не знаешь какому статусу какой приоритет назначить, скажи, я укажу тебе. задачи с ИД "1001" и "1002" и "-" не имеют статус и вид проекта, поэтому они не проблемные и их выводить в списке тоже не нужно
```

Что изменено:

- через Context7 сверена семантика `Set.has()` и предикатов исключения;
- подтверждено, что приоритет статусов уже задан в коде через `getOverallProjectStatus()` и `getProjectStatusPriority()`;
- из диагностики `Задачи для проверки` убрана проблема `несколько статусов`;
- добавлен список служебных ID `1001`, `1002`, `-`;
- строки с этими ID не попадают в диагностику, если у них одновременно не заполнены статус и вид проекта;
- основная таблица проектов и расчет приоритетного статуса не менялись.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 116. Игнорирование статусов вне матрицы проектов

Дата фиксации: 08.07.2026 11:39 MSK

Пользователь уточнила:

```text
если есть проблема с задачей "есть строки со статусом вне общей матрицы: Потенциальная", то мы эти задачи игнорируем и не выводим в ошибку
```

Что изменено:

- через Context7 сверена семантика `Set.has()` для проверки разрешенных значений;
- `shouldIgnoreProjectTableRowByStatus()` теперь игнорирует не только отмененные строки, но и строки со статусом вне общей матрицы проектов;
- статус `Потенциальная` и другие неразрешенные статусы больше не попадают в диагностический блок `Задачи для проверки`;
- удалена диагностика `есть строки со статусом вне общей матрицы`, потому что такие строки теперь исключаются до группировки;
- если у той же задачи есть строки с разрешенными статусами, они продолжают выводиться без строк с неразрешенным статусом.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 117. Ограничение квартальной загрузки максимумом 100%

Дата фиксации: 08.07.2026 14:20 MSK

Пользователь уточнила:

```text
загрузка по кварталам не может быть больше 100%, если она больше, то максимально показывай 100
```

Что изменено:

- через Context7 сверена семантика `Math.min()` / `Math.max()` для ограничения числового значения;
- добавлен helper `clampQuarterlyTrendPercentage()`;
- квартальный процент теперь ограничивается диапазоном `0..100` при сборке данных квартальной динамики;
- дополнительное ограничение применено при выборе последних 4 кварталов, чтобы любые входные данные для графика и бейджа не превышали `100%`;
- расчет часов `loadHours` и `totalHours` не менялся, ограничено только отображаемое значение процента.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- локальный сервер запущен командой `node local-server.mjs`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 118. Заголовок загрузки ТехБлока и превышение часов в целевом показателе

Дата фиксации: 09.07.2026 09:03 MSK

Пользователь попросила:

```text
переименуй на вкладке ТехБлок заголовок "Загрузка центра" в "Загрузка блока", на остальных вкладках оставь без изменений

У целевых показателей, в блоке "В норме часов" укажи сумму часов, которую превысили
```

Что изменено:

- через Context7 сверена работа шаблонных выражений и `Array.reduce()` / числовой агрегации;
- добавлен `updateWorkloadBlockTitle()`;
- на вкладке `ТехБлок` заголовок блока загрузки меняется на `Загрузка блока`;
- на остальных основных вкладках заголовок остается `Загрузка центра`;
- в расчете целевого показателя добавлено поле `hoursOverrun`;
- `hoursOverrun` суммирует превышение `фактические часы - плановые часы` только по завершенным задачам, где факт больше плана;
- в карточке `В норме часов` добавлена строка `превышение: X ч`;
- добавлен стиль `target-indicator-metric__extra` для дополнительной строки в карточке.

Проверка:

- `node --check script.js` прошел успешно;
- баланс фигурных скобок `style.css` проверен через Node: `{"balance":0,"min":0}`;
- локальный сервер запущен командой `node local-server.mjs`;
- `curl -I http://127.0.0.1:8080/` вернул `HTTP/1.1 200 OK`.

## 119. Актуализация файла передачи реализации для другого ИИ

Дата фиксации: 09.07.2026 09:20 MSK

Пользователь попросила:

```text
актуализируй файл Передача_реализации_для_ИИ
```

Что изменено:

- обновлена дата документа `Передача_реализации_для_ИИ.md`;
- добавлены свежие пользовательские решения по квартальной динамике, таблице проектов, диагностике задач, целевому показателю и заголовку `Загрузка блока`;
- раздел источников Excel обновлен под лист `ПП` вместо старого основного имени `План_продаж`;
- описана актуальная структура листа `ПП` с новыми колонками;
- уточнена разница между интерфейсной вкладкой `ПП` и листом Excel `ПП`;
- обновлены правила связи `ПП[Управление] -> центр`;
- переписан раздел по квартальной динамике центров: месячный блок на центрах больше не выводится;
- добавлены разделы `Целевой показатель` и `Таблица Проекты`;
- описаны правила исключения отмененных строк, статусов вне матрицы и служебных ID из диагностики;
- добавлены функции `updateWorkloadBlockTitle()`, `getTargetIndicatorProjectHoursOverrun()`, `clampQuarterlyTrendPercentage()` и функции таблицы проектов в карту функций;
- обновлены чек-листы проверки и список того, что важно не сломать;
- попытка использовать Context7 для Markdown-справки вернула ошибку `Invalid or expired OAuth token`, поэтому актуализация выполнена по локальному коду и истории проекта.

Проверка:

- проверена структура заголовков `Передача_реализации_для_ИИ.md` через `rg '^## '`;
- проверены оставшиеся ссылки на `План_продаж`: остались только упоминания старого fallback-имени листа;
- файл содержит актуальные разделы до `## 27. Рабочий принцип`.

## 120. Аудит текущей папки дашборда

Дата фиксации: 09.07.2026 13:14 MSK

Пользователь попросила аккуратно запустить и проанализировать проект Excel-дашборда в текущей папке без изменения логики.

Что проверено:

- сначала прочитаны файлы `Передача_реализации_для_ИИ (1).md` и `История_переписки (1).md`; исходные имена без суффикса `(1)` в папке отсутствуют;
- в папке найдены `index.php`, `script.js`, `style.css`, `Dashboard_report_Technical_unit.xlsm`, `История_переписки (1).md`, `Передача_реализации_для_ИИ (1).md`;
- `local-server.mjs` в текущей папке отсутствует;
- `node`, `php`, `py` в PATH не найдены, `python` представлен нерабочим Windows alias, поэтому `node --check script.js` и штатный запуск через PHP/Node в этом окружении выполнить не удалось;
- проверен порядок навигации в `index.php`: `ТехБлок -> ЦНГО -> ЦДЦМ -> ЦДПМ -> ЦОТ -> УВП -> ЛОИ -> НК/ТК -> ПП`;
- контейнер `production-program-content` в `index.php` найден;
- в `script.js` найдены ключевые функции и ветка `sheetName === 'ПП'`: `loadSheetData()`, `switchToProductionProgramMode()`, `renderProductionProgramPlanFact()`, `getTechBlockProjectPlanFactDataFromTables()`, `getTechBlockCenterCapacityByMonth()`, `getTechBlockQuarterlyTrendDataFromTables()`, `updateTargetIndicator()`;
- подтверждено, что `productionProgramFactVisible` по умолчанию равен `false`, а скрытие факта на вкладке `ПП` идет через `fact-hidden`;
- подтверждено, что квартальная динамика использует `clampQuarterlyTrendPercentage()` и визуально ограничивает процент диапазоном `0..100`;
- подтверждено, что таблица проектов исключает отмененные строки и статусы вне матрицы через `shouldIgnoreProjectTableRowByStatus()`;
- подтверждено, что расчет фонда часов исключает административный персонал, учитывает отсутствия и даты увольнения;
- в `style.css` найдены стили для фильтров `ПП`, `fact-hidden`, диагностики задач, целевого показателя и квартальной динамики;
- баланс фигурных скобок `style.css` проверен через PowerShell: `{"balance":0,"min":0,"openBraces":1013,"closeBraces":1013}`;
- обнаружен риск: видимой вкладки `Расчеты` в навигации нет, но в `index.php` остается скрытый контейнер `calculations-content`, а в `script.js` остается служебная ветка `sheetName === 'Расчеты'` и `renderCalculationsAudit()`;
- попытки проверить страницу в headless Chrome через временный PowerShell HTTP-сервер завершились таймаутом до возврата диагностического отчета; фоновые процессы после таймаутов не остались.

Код `index.php`, `script.js` и `style.css` в ходе аудита не изменялся.

## 121. Локальный запуск проекта

Дата фиксации: 09.07.2026 13:31 MSK

Пользователь попросила:

```text
Запусти проект
```

Что сделано:

- подтверждено, что `local-server.mjs` в текущей папке отсутствует, а `node` и `php` в PATH недоступны;
- запущен временный локальный PowerShell-сервер на `http://127.0.0.1:8080/`;
- сервер отдает `index.php` как HTML, подставляет ссылки на `style.css` и `script.js`, а Excel-файл доступен по `/file/Dashboard_report_Technical_unit.xlsm`;
- проверено, что главная страница отвечает `HTTP 200`;
- проверено, что `Dashboard_report_Technical_unit.xlsm` отвечает `HTTP 200`;
- дашборд открыт в Chrome по адресу `http://127.0.0.1:8080/`.

Код проекта не изменялся. Временный серверный скрипт находится в системной папке Temp и не является частью проекта.

## 122. Lookup отображаемых названий проектов из листа Названия_проектов

Дата фиксации: 09.07.2026 13:47 MSK

Пользователь попросила:

```text
использовать для отображения названия проекта в таблице проектов лист Названия_проектов
```

Что изменено:

- в `script.js` добавлен отдельный кэш `projectDisplayInfoLookup` в `workbookDataCache`;
- добавлена функция `getProjectDisplayInfoLookup()`, которая читает лист `Названия_проектов` по заголовкам, без привязки к буквам столбцов;
- сопоставление строк идет по нормализованному `ID`;
- добавлена нормализация `normalizeProjectLookupId()`: числа приводятся к строке, пробелы убираются, значения вида `8891.0` приводятся к `8891`;
- из листа `Названия_проектов` подтягиваются поля:
  - `Заказчик, название проекта` / fallback `Заказчик, название проектов`;
  - `Центр`;
  - `Клиент`;
  - `Статус`;
- если по `ID` есть несколько строк, используется первая строка с заполненным названием; если название пустое, остается старое название из `Проекты_ТехБлок`;
- в объектах таблицы проектов добавлены display-поля `displayName`, `displayCenter`, `displayClient`, `displayStatus`;
- группировка, фильтры, статусы матрицы, расчеты часов, дат и индикаторов не менялись: старые поля `name`, `center`, `status` сохранены для логики;
- в раскрывающейся таблице проектов колонка `Название` теперь выводит `displayName`, а при наличии клиента показывает вторую строку `Клиент: ...`;
- в `style.css` добавлен класс `.project-client-note` и усилен перенос длинных названий в `.project-name`.

Изменены файлы:

- `script.js`;
- `style.css`;
- `История_переписки (1).md`.

Проверка:

- Excel-файл `Dashboard_report_Technical_unit.xlsm` открыт как XLSM/ZIP-структура; лист `Названия_проектов` найден;
- подтверждены заголовки листа `Названия_проектов`: `ID`, `Центр`, `Клиент`, `Заказчик, название проекта`, `Статус`;
- в текущем Excel-файле диапазон листа `Названия_проектов` равен `A1:K1`, то есть есть заголовки, но нет строк данных для визуальной проверки совпавших ID;
- `node --check script.js` выполнить не удалось, потому что `node` не найден в PATH текущего окружения;
- баланс фигурных скобок `style.css` проверен через PowerShell: `{"balance":0,"min":0,"openBraces":1014,"closeBraces":1014}`;
- локальный сервер `http://127.0.0.1:8080/` отвечает `HTTP 200`;
- обновленные `script.js` и `style.css` отдаются локальным сервером;
- Excel-файл доступен через локальный сервер по `/file/Dashboard_report_Technical_unit.xlsm`;
- страница дашборда повторно открыта в Chrome;
- автоматическая CDP-проверка консоли Chrome была запущена, но DevTools WebSocket оборвался до возврата отчета, поэтому отсутствие ошибок консоли автоматически не подтверждено.

## 123. Исправление подстановки названий проектов только по B:A

Дата фиксации: 09.07.2026 14:00 MSK

Пользователь уточнила, что предыдущая реализация не меняла значения в колонке `НАЗВАНИЕ` и должна быть проще:

```text
Проекты_ТехБлок!B:B = Названия_проектов!A:A
```

Что исправлено:

- удалена лишняя логика подтягивания `Центр`, `Клиент` и `Статус` из листа `Названия_проектов` для таблицы проектов;
- оставлена только подстановка отображаемого названия проекта;
- добавлен lookup `getProjectNamesLookup()`;
- lookup читает `ID` напрямую из колонки `A` листа `Названия_проектов`;
- название берется по заголовку `Заказчик, название проекта` или `Заказчик, название проектов`;
- в строках `Проекты_ТехБлок` ID для таблицы проектов теперь берется напрямую из колонки `B` той же Excel-строки;
- добавлена неперечисляемая метка строки в `getWorksheetRowsByHeaders()`, чтобы можно было безопасно читать конкретную ячейку исходной Excel-строки;
- добавлен helper `getWorksheetRowCellValue(row, columnIndex)`;
- добавлена нормализация `normalizeProjectIdForLookup()` по правилу: trim, убрать `.0`, убрать пробелы внутри;
- в объект проекта передается только `displayName`;
- рендер колонки `Название` использует `project.displayName || project.name`;
- ID, группировка, центр, статус, даты, часы, остаток, индикаторы, фильтры и вкладка `ПП` не менялись.

Изменены файлы:

- `script.js`;
- `style.css` (удален ранее добавленный стиль строки клиента, сохранен перенос длинных названий);
- `История_переписки (1).md`.

Проверка:

- подтверждено, что в текущем Excel `Проекты_ТехБлок` имеет `ID` в колонке `B`;
- подтверждено, что `Названия_проектов` имеет `ID` в колонке `A` и заголовок `Заказчик, название проекта`;
- в текущем Excel диапазон листа `Названия_проектов` остается `A1:K1`, то есть строк данных для визуальной проверки совпавших ID в этом файле нет;
- `node --check script.js` выполнить не удалось, потому что `node` не найден в PATH;
- баланс фигурных скобок `style.css` проверен через PowerShell: `{"balance":0,"min":0,"openBraces":1013,"closeBraces":1013}`;
- грубый баланс `{}` и `[]` в `script.js` проверен через PowerShell: `{"curly":0,"curlyMin":0,"square":0,"squareMin":0}`;
- локальный сервер отдает обновленный `script.js`, в нем найдены `getProjectNamesLookup()` и чтение `getWorksheetRowCellValue(row, 1)`;
- страница повторно открыта в Chrome по `http://127.0.0.1:8080/`.

## 124. Расчет карточки "Табель" из листа "Загрузка_ТехБлок"

Дата фиксации: 09.07.2026 14:45 MSK

Добавлен расчет карточки `Табель` из листа `Загрузка_ТехБлок`: строки берутся по `ID = "-"` в столбце `I` и сегодняшней дате в столбце `B`. Дата берется динамически из текущей системной даты браузера, заголовок карточки формируется как `Табель 9 июля 2026 г.` для даты 09.07.2026.

Что изменено:

- в `script.js` добавлены helpers `getTodayCalendarDate()`, `isSameCalendarDate()` и `normalizeAbsenceCode()`;
- добавлен безопасный расчет `getTodayAbsenceDataFromTechBlockLoad()`;
- карточка `Табель` больше не берет значения из старых фиксированных диапазонов `EXCEL_CONFIG.absences`, а считает данные по плоскому листу `Загрузка_ТехБлок`;
- дата читается строго из столбца `B`, ID - строго из столбца `I`, тип отсутствия - по заголовку `Отсутствия` с fallback на столбец `M`;
- реализовано сопоставление сокращений `К/к`, `Отп`, `Б/б`, `Обс/обс`, `Ог/ог`, `Декрет/декрет/Д/д` с категориями отсутствий;
- счет идет по уникальным сотрудникам в рамках статуса за день, а строка `Всего отсутствий` считает уникальных сотрудников по всем категориям;
- все шесть строк карточки показываются всегда, даже если значение равно `0`;
- визуальная структура карточки, раскрывающиеся списки сотрудников, блок `Загрузка ресурсов`, таблица проектов, фильтры, группировки и вкладка `ПП` не менялись.

Проверка:

- `node --check script.js` выполнить не удалось: `node` не найден в PATH текущего окружения;
- грубый баланс `{}` и `[]` в `script.js` проверен через PowerShell: значения сошлись в `0`;
- локальный сервер `http://127.0.0.1:8080/` отвечает `HTTP 200`;
- `script.js` и `Dashboard_report_Technical_unit.xlsm` отдаются локальным сервером;
- независимая сверка XLSM через ZIP/XML для даты 09.07.2026 показала: строк с `I = "-"` и датой в `B` - 37, `Отпуск` - 36 уникальных сотрудников, остальные категории - 0, `Всего отсутствий` - 36;
- DOM-проверка в Chrome через CDP показала карточку `Табель 9 июля 2026 г.` и значения: `Командировка 0`, `Отпуск 36`, `Больничный 0`, `Отпуск без сохранения 0`, `Декрет 0`, `Отгул 0`, `Всего отсутствий 36`;
- повторная CDP-проверка загрузки страницы не показала `console.error` / `Runtime.exceptionThrown`; расширенная проверка переключения вкладок была ограничена тяжелым рендером и не вернула результат за окно ожидания, но ошибок JavaScript за время ожидания не пришло.

## 125. Исправление: код "Р" в "Загрузка_ТехБлок" тоже означает "Декрет"

Дата фиксации: 09.07.2026

Точечное исправление предыдущего пункта (124): в `TODAY_ABSENCE_STATUS_MAP` не хватало сокращения `Р/р`, которое в листе `Загрузка_ТехБлок` тоже используется для обозначения декрета, наравне с `Д/д` и `Декрет/декрет`.

Что изменено:

- в `script.js` в словарь `TODAY_ABSENCE_STATUS_MAP` добавлена строка `'р': 'maternityLeave'`;
- остальные сокращения (`к`, `отп`, `б`, `обс`, `ог`, `д`, `декрет`) не тронуты.

Проверка:

- `node --check script.js` - без ошибок;
- прямой разбор `Dashboard_report_Technical_unit.xlsm` (лист `Загрузка_ТехБлок`, столбец `M` - `Отсутствия`) показал, что значение `Р` встречается 245 раз, а `Д` - только 7 раз, то есть `Р` - основной код декрета в этом файле и без исправления карточка `Табель` занижала показатель `Декрет`.

## 126. Порядок блоков в правой колонке и верстка бейджа сверхурочных

Дата фиксации: 09.07.2026

Изменен порядок блоков в правой колонке: теперь сверху отображается "Целевой показатель", ниже "Уведомления по проектам", затем "Табель" и "Загрузка ресурсов". Также исправлена верстка строки "Загрузка в часах" с бейджем "в т.ч. сверхурочно ...", чтобы бейдж не накладывался на подпись.

Что изменено:

- в `index.php` переставлены DOM-блоки внутри `.report_card`: секция `.notification-section` (содержащая `target-indicator-block` и `notification-block`) перенесена перед `.block-employees` (Табель) и `.blocks-container` (Загрузка ресурсов);
- внутри `.notification-section` `target-indicator-block` (Целевой показатель) переставлен перед `notification-block` (рендерится как "Уведомления по проектам"), чтобы итоговый порядок был Целевой показатель -> Уведомления по проектам -> Табель -> Загрузка ресурсов;
- логика скрытия блоков не менялась: `updateTargetIndicator()` и `updateNotification()` по-прежнему сами решают, показывать ли свой блок (`style.display`), поэтому для `УВП`, где целевой показатель скрыт, первым по факту остается блок уведомлений;
- в `script.js` в шаблоне карточки диаграммы (`chart-hours-heading-row`) убран пустой `div.chart-hours-heading-spacer`, у строки добавлен `id="${period.id}-hours-heading-row"`;
- в `createOrUpdateChart()` добавлено переключение класса `chart-hours-heading-row--with-overtime` на этой строке в зависимости от того, показан ли бейдж сверхурочных;
- в `style.css` `.chart-hours-heading-row` переведен с 3-колоночной grid-раскладки (`1fr auto 1fr`, с пустым распорщиком) на 2-колоночную (`minmax(0, 1fr) auto`): заголовок "Загрузка в часах" остается по центру, если бейджа нет, и сдвигается влево классом-модификатором, если бейдж показан; у бейджа и заголовка выставлен `min-width: 0`, чтобы бейдж не выходил за пределы своей колонки и не перекрывал текст;
- добавлен медиа-запрос `@media (max-width: 480px)` для `.chart-hours-heading-row--with-overtime`: колонки становятся одна под другой, бейдж переносится на следующую строку вместо наложения на заголовок.

Проверка:

- `node --check script.js` - без ошибок;
- изменения вносились через побайтовую замену (Python), чтобы не менять построчные окончания (`\r\n`/`\n`) остальной части файлов и оставить diff точечным;
- запущен локальный `php -S` сервер, DOM-проверка через Playwright/Chromium показала порядок дочерних элементов `.report_card`: `notification-section` (Целевой показатель) -> `block-employees` (Табель) -> `blocks-container` (Загрузка ресурсов), и порядок внутри `notification-section`: `target-indicator-block` -> `notification-block`;
- в этом окружении `cdn.sheetjs.com` и `cdn.jsdelivr.net` заблокированы политикой прокси (403), поэтому реальные данные Excel и Chart.js в браузере проверить не удалось; это ограничение окружения, а не следствие изменений;
- верстка строки "Загрузка в часах" / бейджа проверена отдельным статическим HTML-макетом с теми же CSS-правилами и реальной шириной карточки диаграммы (220px и 320px): бейдж и заголовок не пересекаются ни при отсутствии бейджа (заголовок по центру), ни при его наличии (заголовок слева, бейдж справа), ни на мобильной ширине (бейдж переносится на новую строку без наложения) - проверено через Playwright по `getBoundingClientRect()` и скриншотами;
- визуальная структура карточек, названия блоков, расчеты, фильтры и группировки не менялись; блок `Загрузка ресурсов`, таблица проектов и вкладка `ПП` не затронуты.
